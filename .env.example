# Copy this file to .env on the server and edit values there.
# Do not commit .env or real secrets.

GEO_SECRET_KEY=replace-with-a-long-random-secret
GEO_HOST=0.0.0.0
GEO_PORT=5000

# Model credentials stay in data/settings.json through the web UI for now.
