# GEO Agent 工程化说明

## 当前代码结构

- `app.py`：Flask app、路由、权限检查、请求解析和服务模块装配层。
- `services/`：已拆出的业务模块，包括引用情报、crawl jobs、当日统计、原始记录统计、客户资料解析、竞品资料解析和内容生产提示词。
- `templates/index.html`：页面结构和静态资源引用。
- `static/css/app.css`：前端样式。
- `static/js/app.js`：当前前端主体逻辑，仍是一个较大的全局脚本，后续只在收益明确时继续拆。
- 云端部署优先走 Git：本地 commit/push，服务器 `/srv/geo-content-v2` 执行 `git pull --ff-only`，再重启服务并验收。

## 本地环境

推荐使用项目内虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com
python -m playwright install chromium
```

资料上传解析支持 `txt/md/pdf/doc/docx/xlsx/xls`。其中旧 `.doc` 依赖系统工具 `antiword` 或 `catdoc`；Linux/云端可用：

```bash
sudo apt-get update
sudo apt-get install -y antiword catdoc
```

Docker 镜像已经在 `Dockerfile` 里安装这两个工具。`.xls` 仍建议运营转成 `.xlsx` 后上传，解析稳定性更高。

## 启动与服务保护

日常本机启动：

```powershell
.\run_dev.bat
```

`run_dev.bat` 只启动服务，不会自动安装依赖。默认监听 `127.0.0.1:5000`，只允许本机访问。

访问地址：

```text
http://localhost:5000
```

局域网访问：

```powershell
.\启动局域网.bat
```

`启动局域网.bat` 会设置 `GEO_HOST=0.0.0.0` 后调用 `run_dev.bat`。只在可信内网使用，不要暴露到公网。

手动启动：

```powershell
$env:GEO_HOST="127.0.0.1"
$env:GEO_PORT="5000"
.\.venv\Scripts\python.exe -u app.py
```

注意：当前项目可能同时被运营同学使用。没有明确确认前，不要执行 `restart.bat`、`stop.bat`，也不要关闭已经打开的服务窗口或平台登录窗口。

## 进程管理命令

```powershell
.\health_check.bat
.\restart.bat
.\stop.bat
```

说明：

- `health_check.bat` 只做健康检查，适合日常确认服务和登录态。
- `restart.bat` 会清理 `5000` 端口上的旧服务并启动新服务，会影响正在使用的人。
- `stop.bat` 会停止监听 `5000` 的进程并删除 `server.pid`，会直接中断服务。
- `run_dev.bat` 默认监听 `127.0.0.1:5000`。
- `启动局域网.bat` 监听 `0.0.0.0:5000`，同一局域网同事可访问 `http://你的电脑IPv4地址:5000`。

## 测试

主仓库测试：

```powershell
.\run_tests.bat
```

该命令会执行：

- Python 编译检查。
- `unittest` 单元测试。
- Node bridge、平台顺序、登录态判断、任务报告、当日记录等核心行为测试。

单独编译：

```powershell
.\.venv\Scripts\python.exe -m py_compile app.py services\records.py services\node_crawler_bridge.py
```

### 当前测试注意事项

完整验证命令：

```powershell
.\run_tests.bat
```

如只改实体脚本，可单独跑：

```powershell
.\.venv\Scripts\python.exe -m unittest tests.test_history_tools.EntityNormalizeScriptTests -v
```

## 外部 Node 爬虫桥接

主仓库路径：

```text
C:\Users\21295\OneDrive\programing\geo_v2-pro
```

外部 Node 爬虫路径：

```text
C:\Users\21295\OneDrive\programing\ai-search-crawler（进阶API处理）
```

主仓库桥接模块：

```text
services/node_crawler_bridge.py
```

当前 Flask 侧默认支持五个平台走外部 Node 爬虫：

```text
deepseek, yuanbao, qwen, kimi, doubao
```

可用环境变量控制：

```powershell
$env:GEO_NODE_CRAWLER_PLATFORMS="all"
$env:GEO_NODE_CRAWLER_PLATFORMS="none"
$env:GEO_NODE_CRAWLER_PLATFORMS="doubao,qwen"
$env:GEO_NODE_CRAWLER_TIMEOUT="1800"
```

含义：

- `all`：五个平台都走外部 Node 爬虫。
- `none` / `off` / `python`：全部回退 Python 爬虫。
- `doubao,qwen`：只有指定平台走 Node。
- `GEO_NODE_CRAWLER_TIMEOUT`：Node 爬虫总超时秒数。

Node 输出会保存到：

```text
data/tasks/node/<task_id>/<platform>/
```

每次 `/api/platform/crawl` 还会生成任务报告：

```text
data/tasks/YYYY-MM-DD_<task_id>.json
```

## 当前爬取策略

- 当前云端试用推荐链路是：云端页面创建 crawl job，运营电脑运行本地 worker 领取任务，使用运营本机浏览器登录态执行真实爬取，再回传结果入库。
- 问题组页面的 `一键创建爬取任务` 默认创建本地 worker 任务，不再让运营在“云端直接爬”和“交给 worker”之间选择。
- 全平台默认顺序为：DeepSeek -> 元宝 -> 千问 -> Kimi -> 豆包。
- 豆包放最后，降低它卡住或触发风控时对其他平台的影响。
- 后端同一平台仍然串行，不同平台可以并行。云端试用服务仍保持单 gunicorn worker，避免 JSON 文件跨进程写入风险。
- 豆包、Kimi 等平台可能受 VPN、代理、地区风控和高峰限流影响。出现 `verification_required`、服务繁忙或高峰拒绝时，不要连续硬跑；先取消对应云端任务，停止本地 worker，稍后只补爬该平台。

## 运营本地 worker

Windows 运营同学使用打包后的 `GEO-operator-worker-windows-*.zip`，不要把整个开发仓库直接发过去。

关键入口：

- `setup_operator_windows.bat`：首次配置，检查/补齐 Python、Node，检查压缩包内置的 `node_modules`、`ms-playwright` 和登录态目录，完成后进入首次平台登录；不要在运营电脑上现场跑 `npm install` 或 `npx playwright install`。
- `start_local_crawl_worker.bat`：每天启动，要求输入云端用户名和密码，平台默认 all。
- `stop_local_crawl_worker.bat`：停止本机 worker、Node 爬虫和 Playwright 浏览器。
- `导出诊断日志.bat`：导出 `operator_logs` 里的诊断 zip，给技术同学排查。

具体面向运营的说明以 `运营使用说明.md` 为准；入口脚本编码规则以 `docs/engineering-rules.md` 为准。

## 数据文件

关键数据：

- `data/raw_records.json`：细化原始爬取记录，记录库和当日整理主要读取它。
- `data/records.json`：引用情报主分析记录。
- `data/reference_intelligence/<client_id>/<date>/`：引用情报分析阶段产物，包括 `fetched_articles.json`、`stage1_article_structures.json`、`stage2_structure_clusters.json`、`stage3_prompt_plugins.json`。
- `data/reference_intelligence/<client_id>/<date>_all.json`：引用情报分析正式展示和内容生产使用的插件结果。
- `data/tasks/`：每次爬取任务报告。
- `data/raw/<client_id>/<date>.json`：按客户和日期归档的原始数据。
- `data/uploads/`、`data/materials_index.json`、`data/material_cache/`：客户资料解析上传、索引和即时解析缓存。
- `data/material_packages/<client_id>/`：客户资料三阶段 pipeline 与联网补充 Markdown。
- `data/content_uploads/`、`data/content_materials_index.json`、`data/content_material_cache/`：内容生产独立资料上传、索引和解析缓存。
- `data/competitor_material_packages/<client_id>/`：竞品资料上传解析、联网搜索结果和可下载 Markdown。
- `data/platform_login_status.json`：平台登录态摘要。
- `data/*_state.json`：Playwright storage state，包含登录态。
- `data/settings.json`：模型 API 配置，可能包含 API Key。

不要把登录态、API Key 或客户真实数据提交到公开仓库。

如果本地跑出的 `raw_records.json` 有记录缺失在云端，不要整文件覆盖云端数据。用 `scripts/import_missing_raw_records.py` 先 dry-run，再在服务短暂停止时 `--apply` 追加缺失记录。

引用情报分析由页面 `生成引用情报` 触发后台任务。后端会从当日全平台高频引用候选中取 URL，目标抓取 20 篇可用正文；抓取失败的文章直接丢弃并后补。当前进度锚点为正文抓取 5%-30%、阶段一 30%-80%、阶段二 80%-88%、阶段三 88%-99%、完成 100%。正文抓取可能接近 90 秒，运营看到的进度可能会较长时间停在 30% 以内。同一客户、日期、任务范围的重复点击会复用正在运行的任务。

## 实体抽取与归一化脚本

正式脚本：

```text
scripts/normalize_entities.py
```

当前设计原则：

- 双层架构：第一层 LLM 抽取符合行业/业务范围的实体，第二层 LLM 做别名合并和过滤。
- 第一层 prompt 不告诉大模型本客户是谁，避免品牌污染。
- 不强行过滤本客户品牌，自有品牌和竞品一起进入提及榜单，便于对比。
- 后端保持轻量，只负责 JSON 解析、计数、合并、写入和必要兜底。
- 写入字段为 `mentioned_entities`，当日整理的竞品/门店提及从这里聚合。
- 爬虫完成后不自动入库；当日整理页的“生成竞品提及”按钮按当前客户、日期、平台、问题组和批次筛选范围手动排队生成。
- 手动生成只处理 `mentioned_entities` 为空的记录，重复点击不会对已识别记录重复叠加。
- 前端允许人工删除当前筛选范围内的错误实体识别结果。

常用命令：

```powershell
.\.venv\Scripts\python.exe scripts\normalize_entities.py --client-id 20260702143026603814 --date 2026-07-02 --extract-missing --limit 5 --dry-run

.\.venv\Scripts\python.exe scripts\normalize_entities.py --client-id 20260702143026603814 --date 2026-07-02 --extract-missing --apply
```

报告输出到：

```text
reports/entity_normalize_<client_id>_<timestamp>.json
```

脚本包含可测试的抽取、归一化、报告生成和入库函数。相关测试主要在：

```text
tests/test_history_tools.py
tests/test_app_core.py
```

注意：`data/settings.json` 可能包含真实 API Key，不要在日志或对话中泄露完整 key。

## 云端试用运行约束

内容生成云端试用已经进入运行/维护阶段。下面是继续部署或迁移时必须遵守的约束。云端稳定爬虫、多平台发布和复杂权限矩阵都不是第一阶段承诺。

继续部署或迁移时注意：

1. 轻量登录和账号管理：
   - 主页面和业务 API 需要登录。
   - `/api/health` 保持匿名可访问。
   - 密码哈希存储，不写明文密码。
   - 初期运营人数少于 5 人，先不做复杂客户权限矩阵。
2. Linux/Docker 部署准备：
   - `Dockerfile`
   - `docker-compose.yml`
   - `.env.example`
   - `.dockerignore`
   - `deploy/README.md`
3. 单 worker 运行：
   - 当前仍有 JSON、本地文件和 SQLite 混合存储。
   - 云端先用一个 worker，避免多进程写入带来的并发问题。
4. 备份/回滚：
   - 至少覆盖 `data/`、`pdf/`、`logs/`。
   - 内容生成 SQLite 文件也必须纳入备份。

## 外部 Node 项目测试

在外部 Node 项目中运行：

```powershell
cd C:\Users\21295\OneDrive\programing\ai-search-crawler（进阶API处理）
node --test test\bridgeMode.test.js test\doubaoNonInteractive.test.js test\extractionTimeout.test.js test\responseTimeout.test.js test\yuanbaoSidebar.test.js
node --check src\index.js
node --check src\responseTimeout.js
node --check src\adapters\doubaoAdapter.js
node --check src\adapters\yuanbaoAdapter.js
```

## 可清理项

- `__pycache__/`、`tests/__pycache__/`、`scripts/__pycache__/` 是 Python 缓存。
- `server.out.log`、`server.err.log`、`logs/*.log` 是运行日志，可按需归档。
- `reports/` 是自动化回归报告目录，建议保留最近几份用于对比。

清理前确认不会影响正在运行的服务和运营同学使用。
