# GEO Agent 工程化说明

## 本地环境

推荐使用项目内虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com
python -m playwright install chromium
```

## 启动

```powershell
.\启动.bat
```

或手动启动：

```powershell
.\.venv\Scripts\python.exe app.py
```

访问：

```text
http://localhost:5000
```

## 进程管理

```powershell
.\restart.bat
.\stop.bat
.\health_check.bat
.\启动局域网.bat
```

说明：

- `restart.bat` 会先清理 `5000` 端口上的旧服务，再启动新服务。
- `stop.bat` 会停止监听 `5000` 的进程，并删除 `server.pid`。
- `health_check.bat` 会检查端口、`/api/health` 和平台登录状态。
- `scripts/start_server.ps1` 由 `restart.bat` 调用，用于启动服务并写入 `server.pid`。
- 默认服务监听 `0.0.0.0:5000`，同一局域网同事可访问 `http://你的电脑IPv4地址:5000`；只建议在可信内网使用。
- 如需强制仅本机访问，可设置环境变量 `GEO_HOST=127.0.0.1` 后再启动。

## 测试

当前测试使用 Python 标准库 `unittest`，不需要额外安装测试框架。

```powershell
.\run_tests.bat
```

等价命令：

```powershell
.\.venv\Scripts\python.exe -m py_compile app.py base_crawler.py deepseek_crawler.py doubao_crawler.py qwen_crawler.py yuanbao_crawler.py scripts\full_regression.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v
```

测试覆盖范围：

- GEO 评分基础规则
- JSON 原子写入和读取
- `/api/health` 无 API Key 健康检查
- 删除客户时清理关联本地数据
- 未登录平台时爬取接口快速返回 `need_login`
- UTF-8 `storage_state` 登录状态识别

## 无人值守完整回归

如果已经配置 API Key，并且需要测试的平台都已经登录，可以直接运行：

```powershell
.\full_regression.bat
```

默认流程：

- 自动创建临时客户、问题组和发布平台
- 自动选择当前已登录的平台执行 1 轮爬取
- 自动生成 1 篇测试文章
- 自动拉取监测记录、原始记录、文章和统计数据
- 默认删除本次临时客户和发布平台
- 报告写入 `reports/full_regression_*.json` 和 `reports/full_regression_*.md`

常用参数：

```powershell
.\full_regression.bat qwen deepseek --timeout 600
.\full_regression.bat --keep-data
.\full_regression.bat --question "你的自定义测试问题"
```

说明：

- `auto` 模式只测试接口返回为已登录的平台。
- 指定平台时使用平台 ID：`doubao`、`deepseek`、`yuanbao`、`qwen`。
- `--keep-data` 会保留临时客户和发布平台，方便在界面里复查。
- 任何平台爬取失败、文章生成失败或前置检查失败，脚本都会返回非 0 退出码，但仍会尽量写出报告。

## 当前工程化边界

当前阶段可定义为“本地 MVP 工程化闭环”：

- 基础单元测试和语法检查可一键执行。
- Flask 服务可通过脚本重启、停止和健康检查。
- 平台登录态可检查并区分未登录、过期、已验证。
- 可运行无人值守完整回归并生成报告。
- 问题组爬取固定单窗口，优先保证稳定性。

尚未完成的稳定性工程：

- 后台任务队列、任务取消、恢复和重试
- 每次爬取任务独立日志和失败截图
- 引用源提取的跨平台深度优化
- CI 自动化流水线
- 面向运营工作流的 prompt 和页面体验优化

## 可清理项

- `__pycache__/`、`tests/__pycache__/`、`scripts/__pycache__/` 是 Python 缓存，可随时删除。
- `server.out.log`、`server.err.log`、`logs/*.log` 是运行日志，可按需归档或删除。
- `reports/` 是自动化回归报告目录，建议保留最近几份用于对比。
- `login.py` 是历史豆包手动登录脚本，当前主流程已改为前端「平台登录」。暂时保留作备用，不纳入测试编译列表。

## 数据隔离

自动化测试使用临时目录，不读写真实 `data/` 业务数据。真实登录状态文件仍保存在：

```text
data/*_state.json
data/*_cookies.json
```

这些文件包含登录状态，不应提交或外传。
