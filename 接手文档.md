# GEO Agent 项目理解指引

这份文档用于帮助新接手的人快速理解项目在做什么、核心数据如何流转、代码应该从哪里读起。它不是任务清单，也不记录下一步计划。

## 一句话理解

GEO Agent 是一个本地运行的 GEO（Generative Engine Optimization）工作台：围绕客户和品牌，手动维护问题组，批量向多个 AI 平台提问，记录回答和引用源，再把这些数据用于提及率分析、引用源分析、当日整理和内容生产。

## 核心业务闭环

项目的主流程是：

```text
客户管理 → 问题组管理 → 多平台爬取 → 爬取记录库 → 当日数据整理 → 内容生产
```

- 客户管理：维护客户名称、品牌名称、行业/品类和优化目标。
- 问题组管理：按客户业务场景手动录入问题。当前不再使用“探针问题”模块，也不再通过 AI 自动生成问题。
- 多平台爬取：对问题组里的问题发起爬取，默认选择所有已登录平台顺序执行。
- 爬取记录库：保存每次提问的原始回答、引用源、品牌提及、GEO 分数和来源平台。
- 当日数据整理：按日期、问题组、平台范围聚合当天数据，并生成分析结果。
- 内容生产：基于客户资料、模板和分析结论生成内容。

## 平台范围怎么理解

项目支持四个 AI 平台：

```text
doubao / deepseek / yuanbao / qwen
豆包 / DeepSeek / 元宝 / 千问
```

页面右上角的平台选择器现在表示“数据视图范围和默认爬取范围”：

- `全部平台`：默认选项。看板、记录库、当日整理等页面展示所有平台数据；爬取时展开为所有已登录平台。
- 单个平台：只查看该平台数据；问题组爬取时也可以只跑该平台。

问题组爬取区还有一个“爬取范围”选择：

- `所有已登录平台`：默认选项。系统读取 `/api/platform/list`，只对已登录的平台顺序爬取。
- `顶部当前平台`：按右上角当前选择的平台执行；如果顶部是 `全部平台`，仍会展开为所有已登录平台。

后端对 `platform=all` 的语义是“不按平台过滤”。真正调用爬虫时不能把 `all` 发给爬虫，必须先展开成具体平台 ID。

## 数据模型怎么理解

主要数据都在 `data/` 下，本地运行时会读写这些文件：

```text
data/
├── clients.json          # 客户信息
├── probe_groups.json     # 问题组，历史文件名保留
├── raw_records.json      # 爬取记录主库
├── articles.json         # 内容生产结果
├── settings.json         # API Key、模型、数据目录等配置
├── tasks/                # 每次问题组爬取的诊断报告
├── *_state.json          # 各平台登录状态
└── *_cookies.json        # 历史/兼容登录态
```

`raw_records.json` 是分析链路最重要的数据源。每条记录通常包含：

- `client_id`：客户 ID
- `group_id`：问题组 ID
- `question`：本次提问
- `answer`：平台回答
- `refs`：引用源列表
- `source_platform`：爬取来源平台，例如 `deepseek`
- `brand_mentioned`：回答正文是否提到品牌
- `geo_score`：GEO 评分
- `today` / `crawl_time`：日期和爬取时间

## 代码入口怎么读

建议按这个顺序读代码：

1. [app.py](C:/Users/21295/OneDrive/programing/geo_v2-pro/app.py)
   Flask 主入口，包含页面路由、API 路由、分析逻辑和爬取调度。

2. [templates/index.html](C:/Users/21295/OneDrive/programing/geo_v2-pro/templates/index.html)
   单页前端。客户选择、平台视图、问题组管理、记录库、当日整理和内容生产都在这里。

3. [services/records.py](C:/Users/21295/OneDrive/programing/geo_v2-pro/services/records.py)
   原始记录读写、按客户/日期/问题组/平台过滤、删除和清空记录。

4. [services/storage.py](C:/Users/21295/OneDrive/programing/geo_v2-pro/services/storage.py)
   JSON 文件读写封装。

5. [services/crawl_tasks.py](C:/Users/21295/OneDrive/programing/geo_v2-pro/services/crawl_tasks.py)
   爬取任务诊断报告的保存和失败信息压缩。

6. `*_crawler.py` 与 [base_crawler.py](C:/Users/21295/OneDrive/programing/geo_v2-pro/base_crawler.py)
   平台登录、页面操作、回答抓取和引用源提取。

## 关键接口怎么理解

常用 API：

- `GET /api/health`：健康检查，返回数据目录、API Key 状态、平台登录状态。
- `GET /api/platform/list`：返回所有支持平台及登录状态。
- `POST /api/platform/crawl`：单平台爬取接口。前端多平台爬取是顺序多次调用这个接口。
- `GET /api/raw_records`：查询爬取记录库，支持客户、平台、日期、问题组、问题和提及过滤。
- `GET /api/daily/records`：查询某日记录。
- `GET /api/daily/ref_stats`：当日引用源统计。
- `POST /api/daily/deep_analyze`：当日深度分析。
- `POST /api/raw_records/deep_analyze`：记录库筛选后的深度分析。

需要特别注意：`/api/platform/crawl` 只接受具体平台 ID，不接受 `all`。`all` 只适用于查询和前端默认范围。

## 登录态怎么理解

平台登录态由 Playwright 保存到 `data/` 下：

- 豆包：`doubao_cookies.json` 或相关状态文件
- DeepSeek：`deepseek_state.json`
- 元宝：`yuanbao_state.json`
- 千问：`qwen_state.json`

登录状态分为未登录、已过期、已验证。页面中的“所有已登录平台”只会选择已验证的平台。登录态、Cookie、API Key 和业务数据都属于敏感本地数据，不应提交或外传。

## 测试和验证怎么理解

常用命令：

```powershell
.\run_tests.bat
.\restart.bat
.\health_check.bat
.\full_regression.bat deepseek --timeout 600 --keep-data
```

`run_tests.bat` 会做 Python 编译检查和 `unittest` 测试。当前测试覆盖重点包括：

- GEO 评分基础规则
- JSON 读写
- 健康检查
- 客户删除时清理本地数据
- 未登录/登录过期时爬取接口快速返回
- `platform=all` 查询和清空的语义
- storage state 登录状态识别

## 历史命名怎么理解

项目里仍有一些历史命名：

- `probe_groups.json`：现在实际含义是“问题组”，不是探针问题库。
- 部分旧接口或变量名可能仍包含 `probe`、`doubao`，需要结合当前调用链判断是否只是兼容旧命名。
- `F_PROBES` 和探针问题相关历史代码不代表当前产品仍有探针问题模块。

理解这个项目时，以当前页面主流程和 `raw_records.json` 数据链路为准。
