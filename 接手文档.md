# GEO Agent 接手文档

更新时间：2026-07-16

这份文档只保留当前状态、下一步优先级和风险边界。已经完成的历史交接、排障过程和旧计划不再放在这里；需要操作细节时看对应文档。

## 1. 一句话理解项目

GEO Agent 是公司内部使用的 GEO 运营工作台。它以客户为中心，围绕固定问题组向多个 AI 平台提问，保存回答、引用源、品牌/门店提及和任务报告，用于运营复盘、引用情报、记录追溯和内容生产。

当前支持的平台：

```text
DeepSeek -> 元宝 -> 千问 / Qwen -> Kimi -> 豆包
```

豆包放最后，降低登录过期、验证码或地区风控对其他平台的影响。

## 2. 当前状态

- 项目已经进入内部云端试用和维护阶段，不是从零搭建阶段。
- 第一轮可维护性重构已经收口：后端部分业务逻辑已从 `app.py` 拆到 `services/`，前端已拆成 `templates/index.html`、`static/js/app.js`、`static/css/app.css`。
- 云端部署优先走 Git：本地 commit/push 后，服务器 `/srv/geo-content-v2` 执行 `git pull --ff-only`，再重启 `geo-content-v2.service` 并验收。
- 文档不固定最新提交 hash；部署前以 `git log -1 --oneline` 和 `git status --short` 为准。
- 云端仍以 JSON 文件和少量本地文件/SQLite 混合存储为主，继续保持单 gunicorn worker + 多线程，不要为了并发直接开多进程。
- 运营本地 worker 和已发给运营的爬取包不作为当前重构目标；云端负责创建 crawl job 和接收回传数据，真实爬取由运营电脑使用本机浏览器登录态执行。
- 客户资料解析已作为独立模块放在“引用情报分析”和“内容生产”之间；当前支持上传资料、三阶段解析、页面预览、复制和下载 Markdown，内容生产暂不自动使用最终 Markdown。
- 客户资料解析已完成固定 pipeline：机械抽取 -> Filter Worker -> Reducer -> Output Worker。核心文件是 `services/material_package_extractor.py`、`services/material_filter.py`、`services/material_reducer.py`、`services/material_output.py`、`services/material_pipeline.py` 和对应 `scripts/run_material_*.py`。
- `AI联网扩展资料` 已接入真实流程：基于 `data/material_packages/<cid>/latest_injection.md` 生成检索词，调用 Tavily Search，过滤后生成二阶段 Markdown，并保存到 `data/material_packages/<cid>/latest_web_supplement.md`。前端点击按钮会自动跑完整流程，最终只展示二阶段生成内容，并支持复制和下载。

### 2026-07-16 客户资料 AI 联网扩展完成

当前状态：已完成轻量版 AI 联网扩展客户资料模块，并接入前端自动化流程。

已实现：

- Tavily API Key 可在系统设置中长期保存；读取设置时只返回是否已配置，不回显明文 key。
- 后端新增客户资料联网扩展流程，复用现有客户资料解析结果 `latest_injection.md`，生成 6 条左右搜索词，联网检索并筛选命中客户品牌/公司主体的来源。
- 二阶段只生成客户品牌自身可用于 GEO 内容生产的补充资料；客户资料优先级最高，联网信息只补充，不推翻客户资料。
- 生成结果自动落盘到 `data/material_packages/<cid>/latest_web_supplement.md`。
- 前端 `AI联网扩展资料` 按钮已接入自动流程；页面最终只展示二阶段 Markdown，不展示一阶段检索词、来源列表或调试信息。
- 前端支持刷新后读取已保存补充资料、复制和下载 `.md`。

已跑验证：

```powershell
python -m unittest tests.test_frontend_crawl_order -v
python -m unittest tests.test_materials_api tests.test_material_web_expansion -v
python -m unittest tests.test_app_core.FlaskApiTests.test_material_web_expand_requires_existing_injection tests.test_app_core.FlaskApiTests.test_material_web_expand_requires_tavily_key tests.test_app_core.FlaskApiTests.test_material_web_expand_saves_markdown tests.test_app_core.FlaskApiTests.test_material_web_expand_uses_saved_tavily_key_when_env_missing tests.test_app_core.FlaskApiTests.test_download_material_web_supplement -v
```

当前边界：

- 第一版不把联网补充资料自动合并回 `latest_injection.md`，避免污染客户原始资料解析结果。
- 内容生产暂未自动引用 `latest_web_supplement.md`；后续要接入时，应让运营明确选择或在内容生成提示中单独标注“联网补充资料”。
- Tavily 搜索结果过滤要求命中客户品牌名或公司名，目的是减少搜到相似竞品或同名主体。

### 2026-07-15 本地 worker 登录态与豆包并行排查交接

当前状态：已按照 `docs/superpowers/specs/2026-07-15-local-worker-login-concurrency-design.md` 的最小方案完成代码修改和自动化验证，暂不继续扩大代码改动范围，等待下一轮真实浏览器验证。

本轮处理的真实问题：本地 worker 开启同平台双窗口后，豆包任务出现“窗口闪几次、没有继续爬、Total Queries 为 0”的情况。最新一次日志目录是：

```text
logs/local-worker/20260715141818759330/doubao
```

已确认的日志证据：

- 第一次双窗口运行保留在 `node-stdout.log` / `node-stderr.log`，`node-stderr.log` 显示 `worker-2` 报 `need_login: doubao/worker-2 saved state is missing or no longer valid`。
- 第二次重试保留在 `node-stdout-2.log` / `node-stderr-2.log`，`node-stderr-2.log` 显示 `worker-1` 也报同样的 `need_login`。
- 两次生成的 Markdown 都是 `Total Queries: 0`。
- `data/doubao_state.json` 的修改时间停在 2026-07-15 13:33，未随 14:18 的人工登录恢复更新。

根因判断：

- 这次不是豆包接口风控，而是本地登录态文件没有写回到爬虫实际读取的位置。
- Windows/Mac 启动脚本会设置共享 `STORAGE_STATE_PATH=ai-search-crawler/storage/state.json`。
- 但 `services/node_crawler_bridge.py` 爬取时会优先使用 `geo_v2-pro/data/<platform>_state.json`，例如 `data/doubao_state.json`。
- 旧逻辑中，按需登录恢复 `run_login_job()` 没有显式传平台专用 state，可能把新登录态保存到共享 state；随后爬取仍读取旧的 `data/doubao_state.json`，所以重新登录后仍继续 `need_login`。

本轮已改动：

- `scripts/local_crawl_worker.py`
  - 新增同平台并发参数，默认每个平台 job `crawler_concurrency=2`。
  - 登录恢复错误分两类：普通 `need_login` 恢复后继续按配置双窗口；`verification_required`、`captcha`、`账号异常`、`风险验证` 等账号/风控类恢复后只用单窗口重试。
  - `run_login_job()` 现在明确把登录态写回 `ROOT / "data" / f"{platform}_state.json"`，避免写到共享 `STORAGE_STATE_PATH` 后爬取读不到。
- `services/node_crawler_bridge.py`
  - 并发时生成 `accounts.txt`，第一个 worker 用原始平台 state，第二个 worker 用拷贝 state。
  - Node 调用日志不再覆盖：第一次写 `node-stdout.log` / `node-stderr.log`，第二次写 `node-stdout-2.log` / `node-stderr-2.log`，方便排查重试前后的真实失败原因。
- `tests/test_local_crawl_worker.py`
  - 覆盖普通登录恢复继续双窗口、风控恢复单窗口、登录 job 写入平台专用 state。
- `tests/test_node_crawler_bridge.py`
  - 覆盖并发 accounts 文件和 Node 日志不覆盖。
- Mac 打包仓库 `C:\Users\21295\OneDrive\programing\geo-operator-worker-mac`
  - 已同步 `geo_v2-pro/scripts/local_crawl_worker.py`、`geo_v2-pro/services/node_crawler_bridge.py`。
  - 注意：Mac 仓库的 bridge 必须保留 `chrome-mac*/*.app/Contents/MacOS/*` 识别，不能被主仓 Windows bridge 直接覆盖掉。

已跑验证：

```powershell
python -m unittest tests.test_local_crawl_worker tests.test_node_crawler_bridge -v
python -m py_compile scripts\local_crawl_worker.py services\node_crawler_bridge.py
$env:PYTHONPYCACHEPREFIX = Join-Path $env:TEMP 'geo-mac-pycache-final'; python -m unittest discover -s "C:\Users\21295\OneDrive\programing\geo-operator-worker-mac\tests" -v
$env:PYTHONPYCACHEPREFIX = Join-Path $env:TEMP 'geo-mac-pycache-final'; python -m py_compile "C:\Users\21295\OneDrive\programing\geo-operator-worker-mac\geo_v2-pro\scripts\local_crawl_worker.py" "C:\Users\21295\OneDrive\programing\geo-operator-worker-mac\geo_v2-pro\services\node_crawler_bridge.py"
```

当前建议下一步：

1. 先重启正在运行的本地 worker，否则它还在用旧代码。
2. 对豆包执行一次“平台重新登录”或单平台 login job。
3. 登录完成后确认 `data/doubao_state.json` 修改时间变成当前时间。
4. 再跑豆包 crawl job；如果仍失败，优先看最新 `logs/local-worker/<job>/doubao/node-stderr*.log`，不要只看最终 Markdown。
5. 如果 `doubao_state.json` 已更新但仍 `page login check: not ready`，下一步再查豆包适配器 `detectLoginBlocker/isLoggedIn/resolveInput` 是否对新版页面误判。

## 3. 文档分工

- `接手文档.md`：当前状态、下一步优先级、风险边界。
- `工程化说明.md`：本地环境、启动、测试、部署、数据文件、worker 和运行约束。
- `运营使用说明.md`：运营同学如何 setup、启动本地 worker、停止爬虫和导出诊断日志。
- `docs/engineering-rules.md`：入口脚本编码、运营 worker、云端部署和数据补录的硬约束。
- `docs/content-plan.md`：内容生成规则、引用情报如何接入内容生产、短期和长期内容路线。

## 4. 当前主流程

标准运营链路：

```text
客户配置平台 -> 问题组爬取 -> 当日整理 -> 引用/竞品洞察 -> 引用情报分析 -> 客户资料解析 -> 内容生成
```

爬取链路：

1. 云端页面在问题组里创建 crawl job。
2. 运营电脑启动本地 worker 领取任务。
3. 本地 worker 调用外部 Node 爬虫和本机浏览器登录态执行真实平台提问。
4. 本地 worker 只回传回答、引用、失败原因和日志摘要。
5. 云端写入原始记录、任务报告和当日整理数据。

不要把 AI 平台账号、密码、Cookie、Playwright storage state 或浏览器登录态上传到云端。

## 5. 当前能力边界

### 当日数据整理

定位：当前客户当天数据的内部情报汇总。

应展示：

- 当天记录数、引用数、品牌提及率、平台完成情况。
- AI 平台对比：各平台记录数、引用数、品牌提及数、异常数。
- 其他品牌/门店提及统计。
- 高频引用文章。
- 高频引用来源平台。
- 异常数据：零引用、空回答、登录异常、疑似卡住、测试/补爬数据混入。

当日整理按“当天累计数据”作为运营口径；任务批次只用于审计、异常定位和补录追溯，不作为当日整理默认入口。

### 爬取记录库

定位：当前客户的长期数据追溯和审计库。

应展示：

- 按 AI 平台分类查看长期记录。
- 按问题组、日期范围筛选。
- 单条完整证据：问题、AI 平台、完整回答、引用文章、提到的品牌、爬取时间。
- 数据审计：错误记录删除、导出、定位某次任务原始数据。

### 品牌/竞品/门店提及

暂时不维护竞品名单。客户行业变化大，前期维护成本高。

当前处理方式：

- 使用 LLM 从回答正文里抽取被推荐或被提及的门店、品牌、公司名。
- 不强行排除本客户品牌；自有品牌和竞品一起作为“被 AI 提到的实体”展示。
- 抽取结果写入原始记录的 `mentioned_entities`。
- 爬取入库后不自动跑实体识别；当日整理页提供“生成竞品提及”按钮，按当前客户、日期、平台、问题组和批次筛选范围手动排队生成。
- 手动生成只处理 `mentioned_entities` 为空的记录，重复点击不会对已识别记录重复叠加。
- 页面允许人工删除当前筛选范围内的错误实体识别结果，不删除原始回答。

推荐结构：

```json
{
  "entities": [
    {
      "name": "某某汽车音响",
      "type": "门店",
      "sentiment": "positive",
      "evidence": "回答中的短片段"
    }
  ]
}
```

### 引用情报分析

定位：把当日高频引用文章转成可用于内容生产的结构化插件。

当前流程：

1. 从当前客户当天全平台高频引用候选中取 URL，目标抓取 20 篇可用正文。
2. 阶段一 LLM 只看单篇文章标题和正文，抽取开头、正文模块、结尾写法和父类型。
3. 阶段二 LLM 只看阶段一结构摘要，合并相似写法并命名子类型。
4. 阶段三 LLM 只看阶段二结构簇，生成内容生产可插入的插件。

页面只展示三阶段插件和来源文章标题/URL，不展示中间过程给运营。具体内容规则见 `docs/content-plan.md`。

### 内容生产

当前内容生产以两类父类型为主：

- `对比型`：用于“哪家好、推荐、排名、全攻略、怎么选”等内容。
- `介绍型`：用于品牌/服务介绍。

引用情报插件会按 `parent_type` 挂到对应父类型下作为子类型使用。内容生成必须基于客户真实资料、运营意见、样例文章和引用情报，不能编造客户事实、价格、资质、案例或评价。

### 客户资料整理模块

定位：把客户上传的复杂资料包整理成大模型可读、可审核、可复用的客户资料包，再供内容生产选择使用。

本轮讨论后确定的方向：

- 不做重型 Master/Worker 动态编排框架；当前先走固定 pipeline。
- pipeline 顺序是：机械抽取 -> Filter Worker 大量过滤 -> Reducer 裁剪去重 -> Output Worker 生成完整整理资料和内容生产建议。
- Filter Worker 以整个资料包为输入，先做确定性去重，再让模型比较所有候选单元并选择核心资料和少量代表。
- 每个结果只输出一个权威状态 `{"unit_id": "...", "status": "core|representative|..."}`；只有 `core` 和 `representative` 进入下游，不再同时维护 `useful` 布尔值。
- 竞品资料不进入客户资料模块；以后如需要，单独做竞品资料模块，并由内容生产按需启用。
- 同一数据集拆分出的结构相似明细不能全部进入默认资料包；没有汇总单元时最多保留 3 个代表，其余标记为 `reference_only` 或 `redundant`。
- 本地 `pdf/` 导入入口已删除，前端和后端后续都不再维护这个功能；资料入口先只保留网页上传。

已用样例资料：

```text
pdf/翼升学 GEO资料-6月11日汇总版
```

该资料包的当前抽取观察：

- 物理文件约 56 个。
- 当前可读单元约 30 个，其中部分旧 `.doc/.xls` 只是二进制探测文本，不能作为可靠正文。
- 大量图片、旧 `.doc/.xls` 目前只能标记为待 OCR/待转换，不应直接让模型判断有用或没用。
- `.rtf` 当前抽取结果多为 Word/WPS 样式噪声，应归入待重提取，不作为有效文本判断。

当前人工标准答案 v0：

- 默认保留：`翼升学-简介.docx`、`翼升学参数.xlsx::Sheet1`、品牌语料库中客户自身事实/服务 SOP/风控红线部分、`山东电气案例-翼升学.docx`、`河南医药案例-翼升学.docx`、`翼升学山西docx.docx`。
- 默认丢弃：空 sheet、RTF 脏文本、品牌主体不是翼升学的案例、重复价值低的案例、通用模板说明、竞品资料、全国/各省竞品名单。
- 默认不进客户资料包但可后续单独接入：`成考10省简章2026.6.8.xlsx` 各省 sheet。
- 品牌语料库不要整张表直接喂给模型，只保留客户自身事实、服务流程、业务边界和合规红线；模板说明、家居行业占位、空项和未证实强主张要过滤。

Filter Worker 当前会一次查看整个候选包：完整正文只用于本地精确去重，模型读取每个单元最多 1800 字的头、中、尾节选，按 `status` 输出核心、代表、冗余、无关或仅供参考等结果。它只决定整份单元的去留，不裁剪单元内部段落。

#### AI 联网扩展资料（下一个 agent 接手）

当前页面已有 `AI联网扩展资料` 按钮，但只做占位提示，没有后端能力。下一步目标是给客户资料解析模块补一份“有来源的联网补充资料包”，而不是让模型自由编造客户事实。

建议最小闭环：

1. 根据当前客户的品牌名、客户名、行业和已上传资料摘要生成 5-8 个搜索词。
2. 每个搜索词最多取前 3 条结果，总抓取网页控制在 15-20 个以内；每页只读取标题、URL、抓取时间和正文节选。
3. 过滤同名品牌、第三方黄页、旧页面、招商页、明显无关页面；只保留能和客户主体匹配且有可追溯 URL 的信息。
4. 输出单独的 Markdown，例如 `latest_web_supplement.md`，页面可预览、复制、下载；不要自动合并进 `latest_injection.md`，更不要自动喂给内容生产。
5. 所有联网补充信息必须带来源 URL，并用保守表述：`公开页面显示`、`页面介绍`、`该来源提到`。没有来源的内容不进入结果。

实现建议：

- 后端先加轻量同步接口即可，例如 `POST /api/materials/<cid>/expand-web`；不要上重型 Master/Worker、RAG、长期记忆或向量库。
- 可以新增 `services/material_web_expand.py`，保持与现有 `material_pipeline.py` 解耦。
- 前端只改 `expandMaterialPackage()`，让按钮调用接口并把联网补充 Markdown 显示在客户资料解析页。
- 测试至少覆盖：按钮和接口存在、无搜索配置时给清晰错误、有假搜索结果时能生成带 URL 的 Markdown、不会改写已有 `latest_injection.md`。

风险边界：

- 不抓需要登录或绕过反爬的平台页面，不上传 Cookie，不处理小红书/公众号等高风险页面作为第一版。
- 不把第三方页面说法包装成已核实事实；政策、价格、资质、案例和效果承诺都要保守。
- 如果没有可靠搜索 API 或联网能力，先把按钮保持为“暂未接入”，不要做假联网。

## 6. 当前短期优先级

当前短期主线是完善客户资料解析模块，但只做明确小闭环：

1. 等运营反馈当前三阶段资料解析 Markdown 质量，再微调默认提示词和页面呈现。
2. 做 `AI联网扩展资料` 的最小可用版本：有来源 URL、可预览/下载、默认不合并到主资料包。
3. 保持内容生产和资料解析解耦；不要在质量稳定前让内容生产自动读取最终 Markdown 或联网补充资料。

## 7. 长期方向

长期路线只作为方向，不代表当前马上要做。

1. 证据层稳定：爬取、入库、任务报告、错误诊断可靠；每条原始记录都能追溯到 task report 和原始回答。
2. 内部情报层：让运营能从当日整理和记录库看懂 AI 引用了谁、引用了什么、哪个平台变化了。
3. 记录库重组：后续按问题和引用文章看长期变化，而不只是按日期堆记录。
4. 引用规律与模板沉淀：标题规律、文章类型、正文结构、FAQ、对比表、选择标准、客户专属模板。
5. 内容生产与发布闭环：记录发布平台、标题、链接、发布时间、对应问题、是否被 AI 引用、是否提及品牌。

## 8. 暂时不要做

- 不要为了“更干净”继续大规模重构业务逻辑。
- 不要在云端直接改代码；先本地修、测试、commit、push，再让云端拉取同一个提交。
- 不要把云端从单 worker 直接改成多进程。
- 不要整文件覆盖云端 `data/raw_records.json`。
- 不要上传平台 Cookie、storage state、API Key 或客户真实数据到公开仓库。
- 不要在没有运营判断前做优质文章自动评分模型。
- 不要马上上复杂 RAG、复杂记忆系统、复杂权限矩阵、多平台发布或无人值守稳定爬虫。
- 不要在客户资料模块里混入竞品资料；竞品资料以后单独建模块。
- 不要把客户原始资料全量注入内容生产；先经过抽取、过滤、裁剪和人工可审核整理。
- 不要先做 Master/Worker 动态资料编排框架；当前用 pipeline 先跑通。
- 不要为了演示反复大规模爬虫，尤其不要连续请求豆包绕过验证码或风控。

## 9. 数据同步和补录原则

代码同步和数据同步分开处理。

- 代码部署走 Git；Git 不可用时才用 `scripts/deploy_cloud_package.sh`。
- 云端 `data/`、`pdf/`、`logs/`、`.env` 必须保留或备份。
- 本地 `raw_records.json` 有记录缺失在云端时，不要整文件覆盖。
- 缺记录时使用 `scripts/import_missing_raw_records.py`，先 dry-run 看 `append_count` 和 `duplicate_count`，确认后再 `--apply`。
- 真正 `--apply` 前建议短暂停止 `geo-content-v2.service`，避免云端同时写 `raw_records.json`。

## 10. 风险边界

低风险：

- 更新文档。
- 补单元测试。
- 修正错误提示。
- mock Node bridge 验证 Flask API 行为。
- 只读健康检查。

中风险：

- 用真实客户和问题组触发 Node bridge 爬取。
- 修改外部 Node 平台适配器。
- 修改登录态复验逻辑。
- 批量跑某个平台。

高风险：

- 大批量真实爬取多个平台。
- 删除或迁移 `data/`。
- 删除登录态文件。
- 重启或停止正在给运营使用的服务。
- 连续请求豆包绕过验证码或风控。

中高风险操作前必须说明会影响哪些文件、是否写入 `data/`、如何回退。

## 11. 新 agent 开始工作前

1. 先读这份文档，再按任务需要读 `工程化说明.md`、`docs/engineering-rules.md`、`docs/content-plan.md` 或 `运营使用说明.md`。
2. 用 `git status --short` 看当前工作区，不要 reset 或 checkout 用户改动。
3. 用 `git log -1 --oneline` 确认当前提交，不要相信文档里的旧 hash。
4. 如果不需要重启，只做代码和测试，不碰运行中的服务。
5. 如需确认服务状态，优先用 `.\health_check.bat`。
6. 代码改动后跑相关测试；主仓库完整验证用 `.\run_tests.bat`。
7. 真实爬取前确认 VPN/代理状态，尤其是豆包。
8. 任何涉及 `data/`、登录态、API Key、云端服务重启的操作，先说明影响范围和回退方式。

## 12. 常用命令

主仓库测试：

```powershell
.\run_tests.bat
```

健康检查：

```powershell
.\health_check.bat
```

本机启动：

```powershell
.\run_dev.bat
```

手动启动 Node bridge 模式：

```powershell
$env:GEO_NODE_CRAWLER_PLATFORMS="all"
.\.venv\Scripts\python.exe -u app.py
```
