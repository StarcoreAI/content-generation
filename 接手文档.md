# GEO Agent 接手文档

## 0. 2026-07-11 凌晨补充：下一工作日计划（最新）

本节是下一位 agent 或明天继续工作时的第一优先级。当前本地代码已经提交：

```text
075d42d Implement reference intelligence workflow and operator tooling
```

但这批代码还没有同步到云服务器 `/srv/geo-content-v2`。不要误以为云端已经包含 2026-07-10 晚上的引用情报正式流程、运营 worker 修复、部署脚本和文档更新。

### 明天建议按这个顺序做

1. 先把当前本地提交部署到云端，并做轻量验收。
   - 部署前先确认本地 `git status --short` 干净，必要时再跑 `.\run_tests.bat`。
   - 云端部署前先确认 `/srv/geo-content-v2` 的 `data/`、`pdf/`、`logs/`、`.env` 会被保留或备份，不能因为覆盖代码丢客户数据、用户配置、API Key、登录态或内容生成历史。
   - 今天新增的代码还没上云，所以验收必须覆盖：登录、客户/问题组、当日数据整理、引用情报分析页、内容生产子类型、云端创建 crawl job、本地 worker 回传入库。
   - 如果 Git 部署还没配好，可以临时继续用现有 zip + `scripts/deploy_cloud_package.sh` helper；不要手动在 SSH 里复制很长命令，也不要整目录 scp 覆盖云端。

2. 然后优先配置 Git 部署链路，之后再继续重构。
   - 用户原话把 Git 配置列为第三点，但实际执行建议放到重构前：先让云端可以 `git pull` 或从私有远程拉取固定提交，再做大改。否则重构后仍然要靠 scp/zip 传代码，风险更高。
   - 推荐方案：建一个私有 Git 远程仓库，本地 push，云服务器 `/srv/geo-content-v2` 配成 clone/worktree，后续部署走 `git fetch` + `git checkout <commit>` 或 `git pull` + restart。
   - 远程仓库必须继续排除 `data/`、`pdf/`、`logs/`、`operator_logs/`、`deploy_artifacts/`、`.env`、浏览器登录态和 API Key。
   - 云端不要直接改代码。如果必须热修，先在本地修、测试、commit、push，再让云端拉取同一个提交。
   - Git 配好后，可以再补一个小的云端部署脚本，例如 `scripts/deploy_cloud_git.sh`，但不要一开始就做复杂 CI/CD。

3. 最后再做本地和云端代码重构。
   - 当前 `app.py`、`templates/index.html`、爬虫调度、引用情报、内容生产、运营 worker 入口都混在一起，确实已经偏“屎山”。但明天不要大爆炸式重构。
   - 重构顺序要固定：先冻结现有行为和测试，再按模块小步拆分，每一步都能运行 `.\run_tests.bat`，每一步单独 commit。
   - 优先拆边界清楚的模块：引用情报 API/任务状态、crawl jobs/本地 worker API、内容生成 API、当日数据整理、账号和设置。
   - 前端先不要引入新框架；可以先把 `templates/index.html` 里的 JS 按模块抽到静态文件，保持页面行为不变。
   - 不要在同一个 commit 里同时做“文件搬家”和“业务逻辑改动”。先移动，测试通过；再改逻辑。
   - 重构期间云端只部署已经本地测试通过的提交，不要在服务器上边改边试。

### 值得特别叮嘱

- 云端当前仍是试用期文件存储方案，JSON 写入和后台任务不是长期队列系统。多人使用时先保持单 gunicorn worker + 多线程，不要为了性能直接开多进程。
- 引用情报分析会调用大模型和抓正文，耗时可能 5 分钟左右。部署到云端后要确认 gunicorn timeout、线程数和前端轮询不会导致页面误判失败。
- 本地 worker 依赖运营电脑的浏览器登录态，云端只负责发任务和收结果。不要试图把 AI 平台账号、Cookie、storage state 上传到云端。
- `deploy_artifacts/` 是本地生成包目录，已经加入 `.gitignore`，不要提交 zip、数据包或运营日志。
- 明天如果要给运营发新包，必须重新打包，不能拿 2026-07-10 上午以前的旧运营包；否则运营还会遇到缺 Chromium、入口脚本不同步或引用情报未更新的问题。
- 每次部署后都记录：部署提交 hash、部署时间、是否重启服务、验收了哪些页面、有没有补录 `raw_records.json`。否则后面排查会分不清本地和云端代码差异。

## 0. 2026-07-10 引用情报分析正式流程更新（最新）

今天已把“引用情报分析”从实验脚本固化为页面正式流程。当前最新状态以本节为准；下方 2026-07-08/2026-07-09 关于引用情报的一些描述已经是历史背景。

### 今天已经完成

1. 页面入口已固定：
   - 运营进入“引用情报分析”页后，点击右上角 `生成引用情报`。
   - 后端启动后台任务，前端显示简单进度条和 `终止生成` 按钮。
   - 页面最终只展示三阶段产物，也就是可直接进入内容生产的插件，不把一阶段/二阶段中间结果暴露给运营。

2. 正式流程已调整为“传统代码先取证，LLM 分三段做抽象”：
   - 当日数据整理继续负责统计高频引用文章；这件事不再算作引用情报分析的一阶段。
   - 后端从当前客户当天全平台高频引用候选中取最多约 35 个 URL，目标抓取 20 篇可用正文。
   - 正文抓取失败的文章直接丢弃，并从后续候选中补足；成功正文写入 `fetched_articles.json` 缓存，重复生成时优先复用。
   - 正文抓取并行数当前为 3，避免单篇超时拖慢整个流程。

3. 三阶段 LLM 职责已经固定：
   - 阶段一：模型只看单篇文章标题和正文，抽取文章开头写了什么、正文按哪些模块展开、结尾如何收束，并标记父类型 `对比型` / `介绍型`。如果文章详细讲解多个品牌、机构、产品、服务方或方案，即使是逐个介绍，也归为 `对比型`。
   - 阶段二：模型只看阶段一结构摘要，不看正文、URL、平台和引用次数；负责合并相似写法，并给每个结构簇命名子类型。
   - 阶段三：模型只看阶段二结构簇，把它改写成内容生产可直接插入的插件，输出 `parent_type`、`subtype_name`、`prompt_text`、`few_shot`。
   - 阶段三会把默认 `攻略对比型` 插件完整注入为“示例插件”，只让模型学习字段形态、结构颗粒度和展开方式；默认插件本身不删除，仍保留在内容生产里。
   - 阶段三插件是服务当前行业的插件，可以保留当前行业通用词和 A/B/C、A1/A2/A3 这类结构标签，但提示词要求避免未核实的具体数字、价格、人数、年份、排名、分数等详细数据。

4. 前端展示和内容生产接入已经固定：
   - 引用情报分析页展示 `parent_type`、`subtype_name`、`prompt_text`、`few_shot`。
   - 每个插件下方只展示“基于哪些文章合并”的文章标题和 URL；不展示引用次数和 AI 平台。
   - 内容生产按父类型展示引用情报插件：`对比型` 插件只挂在对比型下，`介绍型` 插件只挂在介绍型下；默认 `攻略对比型` 继续保留。

5. 进度条锚点已经按 2026-07-10 真实耗时校准：
   - 最近一次翼程单招正式流程总耗时约 314 秒。
   - 正文抓取耗时约 88 秒，所以后端把正文抓取阶段映射到 5%-30%，运营看到的进度大约 90 秒到 30%。
   - 后续锚点为：阶段一 30%-80%，阶段二 80%-88%，阶段三 88%-99%，完成后 100%。
   - 前端每 200ms 平滑追随后端进度，并允许最多领先后端 4%，避免进度条长时间完全不动；如果大模型长时间没有输出，进度仍会停在当前锚点附近，运营可以点 `终止生成`。

6. 多人使用的最小保护已完成：
   - 同一客户、同一天、同一任务范围如果已经有引用情报分析在跑，再次点击会复用正在跑的后台任务，不会重复启动多份大模型流程。
   - 不同客户或不同日期可以各自排队/运行，当前仍是轻量内存任务状态，不是长期队列系统。

7. 最新翼程单招样本结果：
   - 候选 URL 35 个，实际尝试 28 个，抓取成功可用正文 20 篇，失败 8 篇。
   - 阶段一成功分析 20 篇。
   - 阶段二合并出 11 个结构簇。
   - 阶段三生成 11 个内容生产插件。
   - 这次结果全部是 `对比型`，原因是源文章基本都是榜单、攻略、选择建议和多机构/多方案拆解；代码没有写死只能生成 `对比型`，`介绍型` 仍然支持。

## 0. 2026-07-09 晚间状态更新（下一个 agent 必须先读）

本节覆盖下面上午的 worker 诊断结论。上午问题已经继续修过，下面历史内容只作为排查背景保留，不再代表当前待办优先级。

### 今天晚间已经完成

1. 云端问题组爬取入口已调整：
   - 问题组里的 `一键创建爬取任务` 现在默认创建云端 crawl job，由运营电脑上的本地 worker 领取执行。
   - 旧的“交给本地 worker”独立入口已删除，避免运营分不清两个按钮。
   - 问题组添加问题支持批量粘贴，形如 `1.xxxxx`、`2.xxxxx` 的多行文本会拆成多条问题；没有换行时仍按一条问题添加。

2. 本地运营 worker 已按 Windows 运营包方式整理：
   - 运营首次只需要解压 `GEO-operator-worker-windows-*.zip`，保持 `geo_v2-pro` 和 `ai-search-crawler` 两个文件夹同级。
   - `setup_operator_windows.bat` 负责检查 Python、Node、npm/npx、Playwright Chromium、Node 爬虫依赖和登录态目录；setup 完成后会自动进入首次平台登录流程。
   - `start_local_crawl_worker.bat` 每次启动都会让运营输入云端用户名和密码，平台列表默认写死为 all：`deepseek,yuanbao,qwen,kimi,doubao`，不让运营配置。
   - 日常启动会做较宽松的登录自检：明确识别未登录时阻塞并打开浏览器等待登录；无法可靠判断时不阻塞，避免 Qwen 这类平台误判卡住运营。
   - 本地控制面板提供 `Stop local crawler`，同时保留 `stop_local_crawl_worker.bat` 兜底停止 worker、Node 爬虫和 Playwright 浏览器。

3. 运营日志和诊断已补齐：
   - 所有运营入口都会写入 `operator_logs`。
   - 日志和诊断 zip 默认保留 14 天，下次启动脚本时自动清理过期文件。
   - 运营遇到 setup 卡住、Playwright/Chrome 安装后不往下走、启动失败、平台登录异常时，只需要双击 `导出诊断日志.bat`，把生成的 `GEO-diagnostic-*.zip` 发给技术同学。
   - 诊断包只包含脚本日志、Python/Node/npm/npx 版本、本地爬虫路径和依赖检测结果，不包含平台登录态、Cookie 或密码。

4. 云端同步方式已改成固定 helper：
   - 不再建议往 SSH 里粘贴很长的部署命令。
   - 上传代码包到 `/home/geosystem/` 后，在服务器执行：
     ```bash
     cd /srv/geo-content-v2
     bash scripts/deploy_cloud_package.sh /home/geosystem/<package>.zip
     ```
   - helper 会备份 `data/`、`pdf/`、`logs/`、`.env`，解压覆盖代码，修复属主和基础权限，重启 `geo-content-v2.service`，并等待 `/api/health`。

5. 缺失本地爬虫数据的补录方式已固定：
   - 代码包同步不会自动同步本地 `data/raw_records.json`。
   - 只补缺失原始记录时，使用 `scripts/import_missing_raw_records.py`，先 dry-run，看 `append_count` 和 `duplicate_count`，确认后再 `--apply`。
   - 该工具按记录内容去重，默认追加缺失记录，不覆盖云端已有记录；如果 `record_id` 冲突但内容不同，会给导入记录改新 id 并保留 `import_source_id`。
   - 真正 `--apply` 前建议短暂停止 `geo-content-v2.service`，避免云端同时写 `raw_records.json`。

6. 内容生产侧已完成的关键调整：
   - 引用情报分析插件已插入内容生产模板子类。
   - 对比型下默认选中“攻略对比型”。
   - 内容生成结果上方会标注具体子类型。
   - 用户要求保留的对比型主体展开规则已保留：主体分类对比必须充分展开，占全文多数；不能只有一个对比对象；每个主要类别或对象独立展开，每类至少 2 个代表对象或细分方向，资料允许时优先 3 个。

### 当前建议的下一步

1. 先把最新版代码包同步到云端，使用 `scripts/deploy_cloud_package.sh`，不要再手粘长命令。
2. 如果云端当日数据仍缺最近本地跑过的记录，用 `scripts/import_missing_raw_records.py` 只追加缺失 `raw_records.json` 记录，不要整文件覆盖。
3. 给运营同学发 Windows 运营包时，同时发 `运营使用说明.md`；如果运营电脑出问题，优先让对方双击 `导出诊断日志.bat` 发 zip，而不是让对方打开 PowerShell。
4. 云端爬虫仍按“云端任务中心 + 运营电脑本地浏览器 worker”处理。云端页面负责创建/取消任务和展示数据，本地 worker 负责真实平台登录和爬取。
5. 后续真正要提升稳定性，再考虑把 `crawl_jobs.json`、`raw_records.json` 迁到 SQLite/数据库；当前试用期继续保持单 gunicorn worker、多线程。

## 0.1. 2026-07-09 上午真实云端 worker 验收诊断（历史背景，已被晚间更新覆盖）

2026-07-09 上午已把本地最新版代码同步到云端 `/srv/geo-content-v2`，服务 health 正常，`/api/health` 已能看到 `kimi` 平台。云端代码特征已确认：

- `/srv/geo-content-v2/scripts/local_crawl_worker.py` 包含 `--check`。
- `/srv/geo-content-v2/启动本地爬虫worker.bat` 包含 `STORAGE_STATE_PATH`。
- `/srv/geo-content-v2/接手文档.md` 包含 `189 tests`。

本地 Windows 启动 worker 时发现旧中文入口 `.bat` 会被 Windows `cmd` 按错误编码解析，出现中文乱码被当成命令执行。已在本地修复为：

- 新增 ASCII 入口 `start_local_crawl_worker.bat`。
- `启动本地爬虫worker.bat` 只转发到 `start_local_crawl_worker.bat`。
- 本地 `tests.test_startup_scripts` 已通过；上午当时尚未再次同步到云端，晚间状态以本文件第 0 节为准。

真实云端 worker 验收已跑过一次，结果如下：

```text
job 20260709103134677020 deepseek questions=['你好', 'Hello', '星核引力是什么公司'] result_summary=None persisted_records=0
job 20260709103134905244 yuanbao  questions=['你好', 'Hello', '星核引力是什么公司'] result_summary={'total': 3, 'success': 3} persisted_records=3
job 20260709103134973871 qwen     questions=['你好', 'Hello', '星核引力是什么公司'] result_summary={'total': 3, 'success': 1} persisted_records=1
job 20260709103135202619 kimi     questions=['你好', 'Hello', '星核引力是什么公司'] result_summary=None persisted_records=0
job 20260709103135237591 doubao   questions=['你好', 'Hello', '星核引力是什么公司'] result_summary={'total': 3, 'success': 0} persisted_records=0, error=need_login
```

已确认的问题：

1. 这次不是“1 题 × 5 平台”，而是选中的云端问题组实际有 3 个问题：`你好`、`Hello`、`星核引力是什么公司`。所以当日条数预期不能按 5 条算。下次测试必须新建/选择只有一个正式问题的问题组。
2. Qwen 看起来“重复问”，主要是因为它对 `你好`、`Hello` 触发了 `empty_result` 重试；正式问题 `星核引力是什么公司` 成功并入库 1 条。
3. Kimi 慢不是云端任务中心问题，日志显示 Kimi 在 `Hello` 和正式问题上等待文本稳定较久，最终本地 Markdown 有 3 条结果；但云端 job 仍是 `result_summary=None`，说明它没有成功回传到云端或回传请求失败。
4. DeepSeek 本地 Markdown 日志显示已经跑完 3 条，但云端 job 仍是 `result_summary=None`，同样说明没有成功回传或回传失败。
5. 豆包明确失败于本地登录态失效：`need_login: doubao/worker-1 saved state is missing or no longer valid`。这不是云端入库问题，先重新登录豆包或暂时排除豆包。
6. 云端只入库了 4 条：元宝 3 条、Qwen 1 条。DeepSeek/Kimi 本地爬完但云端未收到结果，这是下一步最重要的 bug。

上午当时的优先排查顺序（历史记录，晚间已被第 0 节覆盖）：

1. 当时建议先不要继续让运营试用 worker。
2. 当时建议先把本地 `start_local_crawl_worker.bat` 修复同步到云端，避免后续包里还保留旧中文 bat 问题。
3. 检查本地 worker 控制台是否对 DeepSeek/Kimi 打印了 `任务回传完成`；如果没有，优先查 `run_platform_loop` 中 `cloud_client.submit_result` 是否抛异常后导致对应平台线程退出。
4. 给 `scripts/local_crawl_worker.py` 增加更明确的 submit 日志和异常日志：开始回传、回传成功、回传失败 HTTP 状态/错误体。
5. 复测时只建一个问题：`星核引力是什么公司？`，平台先选 `yuanbao,qwen,kimi,deepseek`，豆包单独测。
6. worker 创建/领取日志要打印完整问题列表和 `repeat_count`，避免误选问题组后看不出来。
7. 当时还要求云端“交给本地 worker”创建成功后展示每个 job 的问题数和前几个问题；晚间入口已合并为 `一键创建爬取任务`。

## 0.2. 2026-07-08 当前状态更新（历史背景）

今天的主线仍然是“内容生成上云试用”，但为了支持运营后续真实使用，顺手完成了一批账号、配置隔离和爬虫调度改造。现在代码层面已经可以支持多个不同 AI 平台并行爬取；同一个平台仍然串行，避免同平台登录态、风控和浏览器上下文互相冲突。

### 今天已经完成并验证

1. 账号和配置隔离：
   - 已有轻量登录、运营注册入口、登录态保护。
   - `/api/settings` 已改为按当前登录账号保存到 `data/user_settings/<username>.json`。
   - 全局 `data/settings.json` 只作为默认配置/健康检查兜底。
   - 不同运营账号的模型、API Key、base_url、preset 不再互相覆盖。

2. 爬虫体验改造：
   - 当日数据管理/问题组爬取已增加“跑 2 次”选项。
   - 竞品/门店实体识别不再阻塞爬虫主流程：原始数据入库和任务报告保存后，实体处理进入后台线程。
   - 实体结果写回 `raw_records.json` 时按 `record_id` 合并最新文件，避免覆盖后续平台新写入的数据。

3. 多平台爬取并发：
   - 后端从全局爬虫锁改为“按 AI 平台加锁”。
   - 不同平台可以并行爬取；同一平台同时发起第二个爬取会返回 `crawl_busy`。
   - 前端问题组爬取和 Agent 自动任务都改成平台并发池，默认 `2` 个平台并行。
   - 单个平台内部仍然不并行，继续用接口里的 `parallel: 1`。
   - 已把 Kimi 接入主系统平台枚举、客户管理合同平台、问题组自由选择、本地 worker 默认平台和 smoke 工具；排序为 `deepseek/yuanbao/qwen/kimi/doubao`，豆包仍放最后。

4. JSON 写入保护：
   - `services/storage.py` 增加 `update_json`。
   - `records.json`、`raw_records.json`、每日 raw 文件追加写入改为锁内读最新再写回。
   - 这解决的是同一进程内多平台并发写入互相覆盖的问题。
   - 云端仍然建议保持单 worker，直到迁移数据库或引入跨进程文件锁。

5. 最新验证：
   ```text
   .\run_tests.bat -> 189 tests OK
   ```

6. 云端试用服务已上线并完成基础验收：
   - 最新代码已同步到 `/srv/geo-content-v2`。
   - 已创建并启动 `geo-content-v2.service`，运行方式为 conda + gunicorn + systemd，端口 `18080`。
   - 初始 `--workers 1 --threads 1` 在多人访问/慢请求下容易卡住；已调整为单 worker 多线程，当前云端按 `--workers 1 --threads 8` 运行，既避免多进程 JSON 写入风险，又允许少量并发页面请求。
   - 服务器本机 `curl http://127.0.0.1:18080/api/health` 返回 `ok=true`。
   - `curl -I http://127.0.0.1:18080/` 返回 `/login` 跳转。
   - 旧服务端口 `80/443/3000/8000/3101` 未被占用或改动。
   - 已同步用户、客户、问题组、配置、客户资料、内容生成历史、当日爬取数据和 reports；云端页面能看到当日爬取数据，内容生成服务已验证正常。
   - 服务器 UFW 已临时放行当前 SSH 来源 IP 访问 `18080`；运营正式试用前再放行运营同学公网 IP 或调整阿里云安全组。

7. 云端排障必须优先使用 `systematic-debugging`：
   - 遇到“打不开、一直转圈、偶发超时、登录慢、接口无响应”时，不要先猜安全组、域名或直接重启。
   - 先分层验证：服务器本机 `curl --max-time 10 http://127.0.0.1:18080/api/health`、`systemctl status`、`journalctl`、`ss -lntp`、UFW、阿里云安全组、外部网络访问。
   - 只有确认根因后再修；如果只是临时重启恢复，必须记录“重启只是缓解，不是根因修复”。

8. 本地代码当时已补引用情报分析页和实体识别状态展示（历史记录，最新引用情报流程见 2026-07-10 节）：
   - 新增 `GET/POST /api/reference_intelligence/plugins`，按客户、日期、任务范围保存/读取最小插件结果。
   - 新增 `POST /api/reference_intelligence/analyze`，基于当前范围高频引用文章生成并保存 `clusters`、`parent_type`、`subtype_name`、`prompt_text`、`few_shot`。
   - 当时引用情报分析拆成三阶段，仍把“统计高频引用文章”写作一阶段；2026-07-10 已更正为：当日整理负责统计，引用情报正式流程从正文抓取和单篇结构抽取开始。
   - 三阶段 `few_shot` 的生成提示已直接嵌入内容生成“对比型展开 few-shot 示例”，要求模型按同等详细度输出，并强制抽象掉具体机构名、客户品牌、文章名、平台名、年份、具体数据和 URL。
   - 2026-07-08 用翼程单招数据重跑后当时可生成 3 个插件；2026-07-10 正式流程最新结果是 20 篇正文生成 11 个插件。
   - 前端新增“引用情报分析”入口和页面，位置在“当日数据整理”和“内容生产”之间；当时第一版只展示结果，2026-07-10 已正式接入内容生产子类型。
   - 新增 `GET /api/daily/entity_status`，从最近任务报告读取实体识别状态；当日整理“竞品/门店提及”标题旁显示排队、完成、失败或暂无任务。
   - 本地验证：`.\run_tests.bat -> 185 tests OK`。

9. 运营本地爬虫 worker 最小闭环已继续推进：
   - 云端任务中心现在支持 `POST /api/crawl_jobs`、`GET /api/crawl_jobs/next`、`POST /api/crawl_jobs/<job_id>/result`、`POST /api/crawl_jobs/<job_id>/cancel`。
   - 当时问题组页面新增“交给本地 worker”按钮，会按当前问题组、平台和爬取次数创建云端任务；2026-07-09 晚间已合并到 `一键创建爬取任务`。
   - `scripts/local_crawl_worker.py` 长期运行时按平台启动独立 worker 线程，不同平台可以并行领取和执行；同一平台仍然串行。
   - worker 回传成功结果后，云端会写入 `raw_records.json` 和每日 raw 文件；重复回传同一个 job 不会重复入库。
   - 回传 payload 继续过滤 Cookie、storage_state、password、token 等敏感字段。
   - 取消任务已支持 pending/running 状态标记为 `canceled`；pending 任务不会再被领取；已取消任务即使后续收到 worker 回传，也不会写入原始记录。
   - 2026-07-09 已补 worker 自检：`scripts/local_crawl_worker.py --check` 会检查云端健康、外部 Node 爬虫目录、`STORAGE_STATE_PATH` 登录态文件和平台列表；`启动本地爬虫worker.bat` 启动前会先跑自检，自检失败不会进入正式轮询。
   - worker 执行完单个 job、准备回传前会再次检查任务是否已被取消；若已取消则跳过回传。若取消状态检查失败，则继续回传，由云端已有的 canceled-job 防护决定是否入库，避免因为一次状态查询失败丢结果。
   - 2026-07-08 晚间已用临时数据目录跑通 Kimi 小任务闭环：创建 `crawl_job` -> 本地 worker 领取 -> 真实 Kimi 爬取 -> 回传 -> `raw_records.json` 入库；样本问题“星核引力是什么公司？”成功得到回答和引用源。
   - 尚未实现真正远程中断本地浏览器/Node 子进程、worker 心跳、异常 running 自动清理和本地 worker GUI。

### 2026-07-08 当时仍未完成

1. 运营正式试用前还需要开放访问范围：
   - 当前只确认用户本机可以访问 `http://8.160.116.86:18080`。
   - 给运营同学使用前，需要在 UFW/阿里云安全组放行对应公网 IP。
   - 不要改 nginx，不要动旧服务端口 `80/443`。

2. 运营本地爬虫客户端当时还没做完整运营包：
   - 云端现在已具备“云端任务中心 + 运营电脑本地 worker + 本地浏览器爬虫 + 结果回传入库”的第一版闭环，但还没有经过真实运营电脑验收。
   - 不上传 AI 平台账号、密码、Cookie 或浏览器登录态。
   - 云端任务中心使用 `data/crawl_jobs.json`；当前仍是轻量 JSON 方案，不是长期队列系统。
   - 本地 worker 外壳：`scripts/local_crawl_worker.py`，复用现有 `services.node_crawler_bridge.run_node_crawler` 调用外部 Node 爬虫，不抽、不重写 Node 核心。
   - 已新增运营双击入口：`启动本地爬虫worker.bat`。第一版会提示输入云端用户名/密码，默认指向 `http://8.160.116.86:18080`，并默认使用相邻外部 Node 爬虫目录和 `storage/state.json` 登录态；启动前先自检。
   - 交给运营同学前必须做一次真实云端测试：云端页面创建 job -> Windows 本地 worker 自检通过 -> worker 领取真实 job -> 调用真实浏览器爬取 -> 回传 -> 云端当日数据/原始记录可见。不能只用本地临时数据或单元测试代替。
   - 当时还没做真正终止当前本地爬虫进程、任务心跳、异常 running 清理；2026-07-09 已补本地 stop 入口，云端远程强制中断仍不是当前能力。

3. 当时云端代码尚未同步当天最后一版：
   - 云端当前可用，但不一定包含引用情报三阶段、实体状态展示和后续本地修复。
   - 建议同步“已通过 189 tests 的稳定代码包”，同步后只做登录、内容生成、当日数据、引用情报页面、Kimi 平台选项和本地 worker 任务创建/回传入库的轻量验收。
   - 本地 worker 仍未做心跳和远程强制中断；给运营正式试用前要说明这是第一版。

4. 云端稳定化还没系统做：
   - 当前只是把 gunicorn 从单线程调整为少量多线程，解决演示阶段请求互相阻塞的问题。
   - 还需要定位哪些接口慢、哪些请求可能占住线程，优先通过 access log 和 `journalctl` 找证据。
   - 慢任务应继续后台化；长期应把高频写入和任务状态迁到 SQLite/数据库或更可靠的队列。

5. 代码规整还没开始：
   - `app.py` 和 `templates/index.html` 仍然很大。
   - 当前仍不建议在云端试用前做大规模重构。
   - 更合理的顺序是：先上线试用并备份，再按模块拆分内容生成、爬虫调度、数据分析和账号配置。

### 2026-07-08 当时建议的讨论顺序

1. 先完成用户本机云端验收：登录、客户/问题组、当日数据、内容生成。
2. 再决定什么时候给运营同学开放 `18080` 访问范围。
3. 对云端卡顿/超时问题按 `systematic-debugging` 做一次最小稳定化排查，确认是否还有慢接口会占住线程。
4. 如果决定今晚同步云端，先打包并上传当前稳定代码，重启 `geo-content-v2.service` 后做轻量验收。
5. 然后继续完善运营本地爬虫客户端：先做心跳/异常 running 清理，再评估是否抽 Node 内核以支持真正终止当前爬虫进程。
6. 引用情报库/引用情报分析最小闭环当时已本地完成；2026-07-10 已正式接入内容生产，最新状态见文档顶部。
7. 最后讨论代码规整：先拆哪些模块，拆到什么程度，不在上线前做无关大改。

## 0. 2026-07-07 晚间交接更新（下一个 agent 必须先读）

本轮任务临时从“引用情报分析/高频引用文章分类”切到“内容生成上云试用部署”。当前不要继续扩展新功能，明天优先把云端试用服务用最小风险方式跑起来，并且不要影响服务器上已有的旧 GEO 服务和另一个 face-recovery 服务。

### 云服务器部署当前状态

已拿到并验证阿里云 ECS：

```text
公网 IP: 8.160.116.86
私网 IP: 172.18.209.124
hostname: iZ0jl4w2flouy3m8qid1o9Z
SSH 用户: geosystem
本机 SSH 别名: geo-content-v2
项目目录: /srv/geo-content-v2
计划端口: 18080
Python 环境: /home/geosystem/miniconda3/envs/geo-content-v2
```

用户本机已生成 SSH key 并加入服务器 `~/.ssh/authorized_keys`。本机已写入 `C:\Users\21295\.ssh\config`，后续可直接：

```powershell
ssh geo-content-v2
```

注意：用户使用 SakuraCat。开 VPN 时新建 SSH 连接可能被断开；目前可行做法是先关 VPN 连上 SSH，再打开 VPN，已建立的 SSH 会话不会断。不要为了解决这个问题改服务器 sshd 或安全组。

服务器资源和旧服务情况：

- Ubuntu 22.04.5 LTS。
- 4 核，14Gi 内存，根盘约 99G，剩余约 72G。
- 旧服务已占用：`80`、`443`、`3000`、`8000`、`3101`、PostgreSQL、Redis。
- 已有服务包括：`geo-api.service`、`geo-web.service`、`geo-worker.service`、`nginx`、`postgresql@14-main`、`redis-server`。
- 已有目录包括：`/srv/geo-system`、`/srv/geo-system-staging`、`/opt/face-recovery-v2`。

隔离原则：

- 只动 `/srv/geo-content-v2`、`/etc/systemd/system/geo-content-v2.service` 和端口 `18080`。
- 不要动旧项目目录、旧 systemd 服务、`nginx`、Docker、PostgreSQL、Redis、80/443。
- 现阶段不装 Docker。服务器没有 Docker，且为了不影响旧服务，第一版使用 conda + gunicorn + systemd。

已完成的服务器准备：

- 部署包已上传：`/home/geosystem/geo-content-v2-20260707-182422.zip`。
- 已解压到：`/srv/geo-content-v2`。
- 已创建 conda 环境：`geo-content-v2`，Python `3.12.13`。
- 已安装依赖并通过基础 import/compile 检查。
- 已创建 `.env`、`data/`、`pdf/`、`logs/`。
- 临时 gunicorn 测试成功，接口返回：
  - `curl http://127.0.0.1:18080/api/health` -> `ok=true`
  - `curl -I http://127.0.0.1:18080/` -> `302 Location: /login`
- 临时 gunicorn 已停止，最后确认 `18080 not listening`。

明天部署优先步骤见 `deploy/README.md`。最关键是先创建 `geo-content-v2.service`，再创建初始账号，然后才考虑安全组放行 `18080`。

### 明天部署验收标准

1. `ssh geo-content-v2` 能直接登录。
2. `ss -lntp | grep 18080 || echo "18080 not listening"` 部署前确认端口干净。
3. `geo-content-v2.service` 启动后只监听 `18080`，不影响旧端口。
4. 服务器本地：
   ```bash
   curl -s http://127.0.0.1:18080/api/health
   curl -I http://127.0.0.1:18080/
   ```
   `/api/health` 应返回 `ok=true`，首页应跳转 `/login`。
5. 创建首个账号时不要让用户把密码发到聊天里。
6. 公网访问优先通过阿里云安全组只开放 `18080` 给需要访问的人；不要改 nginx。

### 引用情报分析/高频引用文章分类当前状态

注意：本段是 2026-07-07 晚间的历史记录。2026-07-10 已把引用情报分析固化为正式流程，最新状态以文档顶部“2026-07-10 引用情报分析正式流程更新”为准。

历史已完成：

- 当日数据整理里的“高频引用文章”已改成在该页面下按 AI 平台分组展示，每个平台少量展示，当前目标是每个平台约 12 篇；不是在“AI 平台分类”页面里展示高引用文章。
- 新增正文获取能力：`services/article_fetcher.py`，用于从 URL 抓取并解析正文，摘要不够时走正文获取。
- 新增第一阶段结构抽取：`services/article_structure.py`。
- 新增测试：`tests/test_article_structure.py`。
- 第一阶段接口包括：
  - `/api/article_structure/fetch`
  - `/api/article_structure/extract`
- 第一阶段 prompt 已从“硬填结构化表格”改为更自由的结构观察，重点让模型说清楚：
  - 开头如何提出痛点/选择困难。
  - 正文如何组织对比、证据、场景或解决方案。
  - 结尾如何收束和给选择建议。
  - 对内容生产有什么可借鉴变化。
- `parent_type` 不再固定死，但只允许归到两个父类：`对比型` / `介绍型`。不能再扩成“场景型、价格型、榜单型”等父类。
- 子类型可以细分，例如“病情场景匹配型”“资质标准验证型”“价格预算决策型”“榜单分层对比型”。

当时完整测试在第一阶段修正后通过：

```text
.\run_tests.bat -> 160 tests OK
```

2026-07-08 当时状态：

- 页面已经新增并可展示 `parent_type`、`subtype_name`、`prompt_text`、`few_shot`。
- 后端已保存 `clusters`、`plugins`、`source_articles`。
- 当时流程仍写成三阶段：统计高频引用文章 -> 二阶段结构归并 `clusters` -> 三阶段改写为插件。2026-07-10 已更正为：当日整理负责统计，引用情报正式流程从正文抓取和单篇结构抽取开始。
- 翼程单招样本当时能生成 3 个插件；2026-07-10 最新正式结果是 20 篇正文生成 11 个插件。
- 2026-07-09 已把插件作为内容生产子类型接入；插件会按 `parent_type` 展示在 `对比型` 或 `介绍型` 下，对比型默认保留“攻略对比型”。

重要诊断：PowerShell 管道给 LLM 脚本传中文前需要设置 UTF-8，否则中文会变成 `????`，导致二阶段模型误解。

```powershell
$OutputEncoding = [System.Text.UTF8Encoding]::new()
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
```

## 0. 2026-07-06 最新交接更新（下一个 agent 必须先读）

本窗口主要完成了“内容生成上云试用”前的前端瘦身、内容生成链路整理和提示词迭代。下一个 agent 的首要任务不是继续扩展新功能，而是继续完成云服务器上线准备：轻量账号登录、部署配置、备份/回滚文档和上线验收。云服务器账号目前还没有交付，拿到账号前先把本地可验证的准备工作做完。

### 当前已完成的关键变化

1. 前端已删除不再使用的旧模块入口，不是隐藏：
   - 数据总览
   - AI引用情报
   - 数据看板
   - 平台库工具箱
   - 旧 `/api/articles/*` 多平台文章生成/审核/格式化残留
2. 默认进入页面已改为“问题组管理”。
3. 已修复客户切换 bug：在问题组管理中打开某客户的问题组后，右上角切换客户会清掉旧客户的问题组详情，不再混显示。
4. 内容生成历史已迁入 SQLite：
   - 主要文件：`services/content_generations.py`
   - 生成记录按客户隔离。
   - 对比型和介绍型的上下文历史互相隔离。
   - 页面展示结果仍合并展示，按时间倒序排列。
5. 内容生成页面已改成运营显式选择文章类型：
   - `对比型`
   - `介绍型`
   - 后端不再从运营意见文本里解析文章类型。
6. 内容生成结果卡片现在显示：
   - 实际调用模型，例如 `deepseek-chat` / `deepseek-v4-pro`
   - 文章类型
7. 兔博士旧生成内容已清理过一次；后续不要误以为早期测试稿仍有保留价值。
8. 启动方式已规整：
   - 本机日常启动：`.\run_dev.bat`
   - 局域网演示：`.\启动局域网.bat`
   - `启动.bat` 已删除。
   - `restart.bat` 仍存在，但会影响正在使用的人，使用前必须确认。

### 内容生成提示词当前状态

当前只做两类文章，不继续细分太多类型，避免运营分不清：

- `对比型`：标题严格模仿高引用文章的“城市/项目/全攻略/推荐/怎么选/年份”结构；正文是少量攻略开头 + 大量分类对比/排名/适合人群/优缺点/快速匹配 + 少量避坑总结；明确要求不能只有一个对比对象。
- `介绍型`：标题必须包含品牌名；正文是少量攻略型开头 + 大量品牌结构化介绍 + 少量选择建议/注意事项；最新要求是按“用户痛点/顾虑 -> 品牌如何解决 -> 资料中的证据支撑”组织，资历、资质、团队、流程、设备、服务记录只能作为证据，不能堆履历。

兔博士高频引用文章已下载到：

```text
reports/reference_article_1046143123.html
reports/reference_article_1046143123.txt
```

对应搜狐文章：

```text
https://m.sohu.com/a/1046143123_122828553/
```

这篇文章主要是“少量攻略 + 大量对比”的结构。后续如果继续优化对比型 prompt，可以优先参考它。

### 最近一次人工评估结论

最新一篇兔博士介绍型文章：

```text
id: 20260706172905665688
type: 介绍型
model: deepseek-v4-pro
title: 西安兔博士口腔牙齿矫正怎么样？从医生团队到就诊流程，一次说清楚
```

当前判断：可以发给运营同学判断。它已经比上一版更像介绍型内容：开头有用户痛点，主体按正规性、医生团队、服务方式、复诊流程回应顾虑，硬广感下降。仍需运营重点核对医生履历、资质、案例数、上市/医保定点等医疗事实是否真实可发布。

如果运营觉得仍像品牌宣传，下一步不要加新流程，先继续压缩介绍型 prompt：要求每个医生或资质信息都必须对应一个用户关心的问题，不能连续堆资料。

### 最近验证结果

2026-07-06 最新完整测试：

```text
.\run_tests.bat -> 133 tests OK
```

注意：PowerShell 输出中文可能乱码，但测试实际通过。

### 下一个 agent 的优先级

拿到云服务器账号前，继续做本地可验证的上线准备：

1. 先读 `docs/superpowers/plans/2026-07-06-content-generation-cloud-rollout.md`。
2. 做轻量登录和账号管理：
   - 所有主页面和业务 API 默认需要登录。
   - `/api/health` 可匿名访问。
   - 密码必须哈希存储，不要明文。
   - 初期只需要内部不到 5 人使用，不要做复杂权限矩阵。
   - 先允许所有已登录运营看到所有客户；后续再做账号-客户隔离。
3. 做部署准备：
   - `Dockerfile`
   - `docker-compose.yml`
   - `.env.example`
   - `.dockerignore`
   - `deploy/README.md`
   - 单 worker 启动，避免 JSON/本地文件写入并发问题。
4. 做备份/回滚说明：
   - `data/`
   - `pdf/`
   - `logs/`
   - 内容生成 SQLite 文件
5. 跑全量测试后再让用户重启服务。

暂时不要做：

- 不要继续动爬虫内部逻辑。
- 不要做大规模 `app.py` / `templates/index.html` 重构。
- 不要引入复杂角色权限、审批流、多租户隔离。
- 不要把云服务器部署与稳定云端爬虫绑定在一起；短期云端先保证内容生成可用。

## 0. 2026-07-06 今日交付方向（先读）

本周进入“内容生成上云试用”交付模式。周三前优先把内容生成模块做成公司内部可用版本：登录访问、客户资料上传/PDF 解析、样例文章参考、多轮改稿、历史文章查看和基础备份。爬虫、高频引用文章仍然重要，但周三不承诺“多人云端稳定爬虫”；短期优先复用同事或指定本地稳定机器的爬虫数据，云端先保证内容生成可用。

今日执行计划见：

```text
docs/superpowers/plans/2026-07-06-content-generation-cloud-rollout.md
```

当前阶段不要做大规模重构。允许的规整只限于：轻量登录、JSON 写入保护、部署/Docker 配置、环境变量和上线检查清单。`app.py` 和 `templates/index.html` 确实偏大，但周三前不要为了“代码好看”破坏已经可用的链路。

## 0. 2026-07-03 最新交接更新（必须先读）

本轮已经完成从“证据层稳定”到“内容生成 MVP”的一组关键改造：竞品/门店实体抽取入库、优质竞品宣传文章展示、客户合同平台配置、爬虫原始记录兜底保存、以及内容生产的简化多轮生成流程都已经接入。下一步不要继续零散加功能，应该先和用户讨论这个项目后续怎么长期优化、怎么分阶段产品化、哪些能力值得沉淀成稳定流程。

当前最重要的接手原则：先围绕“运营怎么使用、客户怎么复盘、内容生成怎么闭环”讨论长期路线，不要在没有确认产品方向前继续扩展复杂自动化、复杂记忆系统或大规模爬取链路。

### 已完成并已验证的功能改动

1. 当日整理的“平台引用权重”已经改成按引用条数统计，平台 count 加总应与 `total_refs` / 总引用文章数对齐。
2. “引用来源平台 Top”这个重复的全局模块已从当日整理页面删除。
3. “AI 平台分类”下已经新增每个 AI 的来源平台分布，口径为引用条数。
4. 高频引用文章 Top20 已新增“被哪些 AI 引用”的展示。
5. 高频引用文章去重已改为 canonical key：
   - 今日头条 `/article/<id>` 与 `/a<id>` 会合并为同一篇文章。
   - 普通 URL 会去掉 query / fragment。
   - 没有 URL 时用归一化标题兜底。
   - 相关文件：`services/ref_articles.py`、`app.py`、`services/record_insights.py`。
6. 引用来源平台名称已增加聚合层归一化：
   - 例如历史数据里的 `com` 会尽量从 URL 反推成 `pcauto`、`mmsonline` 等。
   - 相关文件：`services/ref_platforms.py`。
7. 竞品/门店实体抽取与归一化脚本已实现并接入：
   - `scripts/normalize_entities.py` 支持 `--extract-missing --dry-run`、`--apply`、`--apply-report`。
   - 第一层 LLM 批量抽取符合行业范围的实体，只保留实体名、证据、业务行业信息。
   - 第二层 LLM 负责别名合并和过滤，输出轻量 canonical entities。
   - 写入 `raw_records.json` 的字段是 `mentioned_entities`，当日整理页面据此聚合竞品/门店提及。
   - 爬虫完成后会对本次新爬任务做增量实体入库，不再每次重跑全量。
8. 前端已经支持人工删除竞品展示界面的实体：
   - 删除的是当前筛选范围内聚合实体对应的 `mentioned_entities` 项。
   - 不删除原始回答，也不做单条回答级实体编辑。
9. 优质竞品宣传文章展示已完成：
   - 当日整理会在高频引用 Top20 里标注是否提到目标竞品。
   - 已支持正文命中检查结果入库和前端展示。
   - 已新增“优质竞品宣传文章（人工挑选）”展示区，用于运营判断价值。
10. 问题组爬取平台选择已改造：
   - 全局右上角不再提供“全部平台”筛选。
   - 客户管理支持配置合同约定平台。
   - 问题组管理可选择使用客户配置或自由选择平台。
11. 爬虫入库稳健性已增强：
   - 原始记录先保存，不因 AI 分析失败丢失记录。
   - AI 分析失败会重试；重试仍失败时使用基础分析规则兜底。
12. 内容生产 MVP 已改造：
   - 当前客户上传资料默认全部参与生成，不再逐项勾选。
   - 支持粘贴优质样例文章链接。
   - 支持从当天高频引用 Top20 勾选样例文章。
   - 只保留一个运营意见框和生成按钮，调用 `deepseek-pro`。
   - 支持多轮生成，历史运营意见和历史文章会作为上下文输入。
   - 结果区按生成时间倒序展示过往文章。
13. 最近一次完整验证：
   ```text
   .\run_tests.bat -> 96 tests OK
   ```

### 实体模块当前口径

实体模块现在采用两层 LLM + 轻量后端处理：

```text
第一层：LLM 从 AI 回答正文中抽取符合行业/业务范围的公司、品牌、门店实体
第二层：LLM 统一名称、合并别名、过滤不相关实体
后端：解析 JSON、计数、写入 mentioned_entities、聚合展示
```

关键原则：

- 不把本客户品牌从榜单里强行过滤掉，自有品牌和竞品一起作为“被 AI 提到的实体”展示。
- 第一层 prompt 不告诉大模型本客户是谁，避免品牌污染抽取。
- 第二层直接输出保留实体，不输出 rejected list，减少报告噪声。
- 每天跑新爬虫后，只对本次新记录做增量抽取入库，不重跑全量。
- 页面允许人工删除错误实体；这只是修正榜单，不删除原始回答。

### 实体脚本常用命令

```powershell
.\.venv\Scripts\python.exe scripts\normalize_entities.py --client-id 20260702143026603814 --date 2026-07-02 --extract-missing --limit 5 --dry-run

.\.venv\Scripts\python.exe scripts\normalize_entities.py --client-id 20260702143026603814 --date 2026-07-02 --extract-missing --apply
```

### 下一个 agent 推荐工作：长期优化方案讨论

目标：不要马上继续写新功能，先把项目后续优化路线和用户讨论清楚，形成阶段性产品路线。当前系统已经能完成“爬取 -> 证据整理 -> 竞品/引用洞察 -> 内容生成 MVP”，下一步需要判断哪些能力应该稳定化、哪些能力只是演示验证。

建议讨论重点：

1. 运营演示流程：
   - 一个客户每天从哪里开始。
   - 爬取完成后运营先看哪些指标。
   - 哪些信息用于给客户汇报，哪些信息用于内部生产。
   - 内容生产的输入、审核和输出应该如何落地。
2. 数据模型沉淀：
   - 客户、合同平台、问题组、任务批次、原始记录、实体、引用文章、内容生成记录之间的边界。
   - 哪些数据需要长期入库，哪些只是当天临时分析。
   - 当前 JSON 文件方案什么时候需要迁移到数据库。
3. 证据层可靠性：
   - 爬虫失败、AI 分析失败、平台风控、重复批次、零引用记录的标准处理流程。
   - 每次爬取必须能追溯到 task report 和原始回答。
4. 引用文章价值评估：
   - 高频引用文章、优质竞品宣传文章、未提竞品文章分别对运营有什么价值。
   - 是否需要正文抓取、正文摘要、人工标注和长期沉淀。
5. 内容生产长期方案：
   - PDF 客户资料、样例文章、Top20 引用文章、运营意见、历史生成内容的权重如何设计。
   - 是否需要模板库、客户禁忌词、事实校验、生成版本对比、人工审核状态。
   - 多轮对话是否继续用全量历史，还是改成摘要记忆。
6. 产品化边界：
   - 哪些页面是给运营内部用，哪些结果可以直接导出给客户。
   - 是否需要权限、客户隔离、导出报告、定时任务和通知。

暂时不要做：

- 不要马上引入复杂记忆管理。
- 不要马上重构成大数据库项目。
- 不要在没有运营判断前做优质文章自动评分模型。
- 不要为了演示反复大规模爬虫。
- 不要继续把页面堆功能，先确认长期信息架构。

### 注意事项

- 不要随意重启服务，除非用户明确要求。用户刚说服务已经重启且页面没问题。
- 写入 `data/raw_records.json` 前仍要明确说明用途；实体自动增量入库已经接入爬虫完成流程。
- `data/settings.json` 包含真实 API Key，不要在回复里泄露完整 key。
- DeepSeek API dry-run 期间可能出现网络错误，例如：
  ```text
  APIConnectionError: [SSL: WRONG_VERSION_NUMBER] wrong version number
  ```
  这种情况不代表脚本设计错误，可以让用户本机重跑命令或稍后重试。

更新时间：2026-07-03

这份文档是新 agent 接手 `geo_v2-pro` 的唯一入口。原来的“任务交接”信息已经合并到这里，不再维护第二份交接文档。

## 1. 一句话理解项目

GEO Agent 是一个公司内部使用的本地 GEO 工作台。它先选定客户，再围绕客户的固定问题组，批量向多个 AI 平台提问，保存回答、引用源、品牌提及和任务报告，用于运营复盘、引用情报、记录追溯和后续内容生产。

当前运营核心要求：DeepSeek、豆包、元宝、千问、Kimi 五个平台都要接入真实爬取链路，并能对固定问题组完成全平台测试。

## 2. 当前工作重心

当前阶段已经不只是“把证据层跑稳”。证据层、实体层、引用洞察和内容生产 MVP 都已经具备基础能力。接下来工作重心是产品化讨论和长期优化设计：

- 明确运营每天怎么用这套系统。
- 明确客户汇报需要哪些稳定指标。
- 明确哪些数据要长期沉淀。
- 明确内容生产从“能生成”到“可审核、可复用、可追踪”的路线。
- 在进入大规模重构前，先把产品边界和数据边界讨论清楚。

## 3. 项目路径

主仓库：

```text
C:\Users\21295\OneDrive\programing\geo_v2-pro
```

外部 Node 爬虫：

```text
C:\Users\21295\OneDrive\programing\ai-search-crawler（进阶API处理）
```

外部 Node 项目不是本仓库的一部分，提交主仓库时不会自动提交它的改动。

## 4. 平台范围与顺序

当前支持五个平台：

```text
deepseek -> DeepSeek
yuanbao  -> 元宝
qwen     -> 千问 / Qwen
kimi     -> Kimi
doubao   -> 豆包
```

全平台爬取顺序已经调整为：

```text
DeepSeek -> 元宝 -> 千问 -> Kimi -> 豆包
```

豆包放最后是为了降低它登录过期、卡住或触发风控时对其他平台的影响。

## 5. 当前服务状态注意事项

服务已经被运营同学使用时，不要随意执行：

```powershell
.\restart.bat
.\stop.bat
```

也不要关闭当前服务窗口或平台登录窗口。需要重启前必须先确认。

只读检查可以使用：

```powershell
.\health_check.bat
```

健康检查以当前 `/api/health` 和平台登录状态接口为准。历史只读健康检查曾显示：

- Flask 服务正常。
- DeepSeek、豆包、元宝、千问登录态均为 `ok`。
- DeepSeek API Key 已配置，`has_api_key=true`。

## 6. 已完成的主要代码工作

主仓库已完成：

- 新增 `services/node_crawler_bridge.py`。
- `/api/platform/crawl` 默认支持五个平台走外部 Node 爬虫。
- 支持 `GEO_NODE_CRAWLER_PLATFORMS` 控制 Node / Python 爬虫路径。
- Node 输出保存到 `data/tasks/node/<task_id>/<platform>/`。
- 任务报告记录 `crawler_engine`。
- Node 登录失效映射为 `cookie_expired`。
- 平台验证码/限流映射为 `verification_required`。
- 前端全平台爬取顺序改为 DeepSeek、元宝、千问、Kimi、豆包。
- 后端从全局锁改为按平台串行；同一平台不并行，不同平台可以并行。云端试用仍保持单 gunicorn worker，避免 JSON 跨进程写入风险。
- 修复汇总阶段品牌提及可能被 800 字截断低估的问题。
- 增加 `scripts/crawler_smoke.py`，用于命令行小样本真实爬取验证。

外部 Node 爬虫已完成：

- `src/bridgeMode.js`
- `src/extractionTimeout.js`
- `src/responseTimeout.js`
- 豆包登录态、发送按钮、SSE 引用、验证码/限流检测修复。
- 元宝引用侧边栏提取后自动关闭。
- 豆包 bridge 模式响应超时，避免无限卡住。

## 7. 最近真实测试结果

客户：

```text
扬州苏韵汽车音响
```

客户 ID：

```text
20260701152718423132
```

问题组 ID：

```text
20260701152826201973
```

最新完整全平台 10 题测试时间窗口：

```text
2026-07-02 10:53 - 11:26
```

结果：

| 平台 | 记录数 | 原始引用数 | 零引用记录 | 品牌提及 |
| --- | ---: | ---: | ---: | ---: |
| DeepSeek | 10 | 99 | 0 | 0 |
| 元宝 | 10 | 92 | 0 | 0 |
| 千问 | 10 | 60 | 2 | 0 |
| 豆包 | 10 | 100 | 0 | 5 |

历史完整批次样本合计：

```text
40 条原始记录，351 个原始引用，5 条品牌提及
```

当天累计数据（当前运营整理口径）：

```text
66 条原始记录，546 个引用
```

差异原因：当天累计包含早些时候的小样本和中断测试，不等于上面的历史完整批次样本。

当前产品决策：运营只跑“当天数据整理”，不再要求当日整理按单次任务单独汇总。任务批次仍保留为记录库审计、异常定位和补录追溯字段，不作为当日整理的默认入口。

## 8. 已知数据质量问题

1. 千问有 2 条零引用记录：
   - `扬州新能源汽车音响改装店推荐？`
   - `准备对汽车音响进行改造升级，求推荐几家扬州当地专业可靠的汽车音响公司`

   后续需要判断是平台本身没给引用，还是 qwen 引用解析漏提取。

2. 任务报告里的引用数和 `raw_records.json` 的引用数可能不完全一致。

   原因：任务报告中的 `success.ref_count` 是合并后按标题去重的分析引用；原始记录是单题平台返回引用。

3. 豆包可能受 VPN、代理、地区风控影响。

   出现 `verification_required` 或 `rate limited` 时不要连续硬跑，优先让用户关闭 VPN 后手动触发测试。

4. 当前运行中的服务未必加载了最新代码改动。

   如果代码已改但未重启，线上页面仍使用旧逻辑。重启前必须确认不会影响运营同学。

## 9. 当日数据整理与爬取记录库的产品边界

系统是先固定客户，再进入问题组、记录库和当日整理。因此这两个模块内部不需要再做客户检索。

### 当日数据整理

定位：当前客户当天数据的内部情报汇总。

应展示：

- 当天记录数、引用数、品牌提及率、平台完成情况。
- AI 平台对比：各平台记录数、引用数、品牌提及数、异常数。
- 其他品牌/门店提及统计：不维护竞品名单，优先用 LLM 从回答正文抽取。
- 高频引用文章。
- 高频引用来源平台。
- 异常数据：零引用、空回答、登录异常、疑似卡住、当天测试/补爬数据混入。

不放在这里：

- 按问题诊断。
- 下一步优化建议。
- 引用内容规律、标题规律、文章类型规律。

### 爬取记录库

定位：当前客户的长期数据追溯和审计库。

应展示：

- 按 AI 平台分类查看长期记录。
- 按问题组、日期范围筛选；任务批次仅用于审计、异常定位和补录追溯。
- 单条完整证据：问题、AI 平台、完整回答、引用文章、提到的品牌、爬取时间。
- 长期变化：品牌提及率、引用数、其他品牌出现频率、引用来源平台变化。
- 数据审计：错误记录删除、导出、定位某次任务原始数据。

不放在这里：

- 客户检索。
- 内容规律总结。
- 优化建议。

## 10. 品牌/竞品/门店实体抽取现状

暂时不维护竞品名单。原因是客户行业多变，前期维护成本高。

当前已经用现有 DeepSeek API 做 LLM 抽取并写入原始记录：

- 爬取入库后抽取回答中被推荐或被提及的门店、品牌、公司名。
- 不强行排除本品牌；自有品牌和竞品一起作为“被 AI 提到的实体”展示，便于对比。
- 后端只做 JSON 解析、计数、合并、写入和轻量兜底，避免复杂规则误导后续维护。
- 抽取结果写入原始记录，例如 `mentioned_entities`。
- 页面只聚合已保存结果，不要每次打开页面都重新调 LLM。
- 爬虫完成后自动对本次新记录做增量实体入库。
- 页面允许人工删除错误实体；删除的是当前筛选范围内的实体识别结果，不删除原始回答。

推荐 JSON 结构：

```json
{
  "entities": [
    {
      "name": "某某汽车音响",
      "type": "门店",
      "sentiment": "positive",
      "evidence": "回答中的短片段"
    }
  ]
}
```

## 11. 短期计划

优先级从高到低：

1. 云端同步最新代码并启动正式 `geo-content-v2.service`，先跑通登录、内容生成和数据目录。
2. 确认本地到云端的数据同步清单，优先同步用户、客户、问题组、内容生成历史、客户资料和 `data/user_settings/`。
3. 做运营本地爬虫客户端方案，云端只做任务中心和数据中心，运营电脑负责使用本机浏览器登录态执行爬虫并回传结果：
   - 云端创建待爬任务，记录客户、问题组、AI 平台、任务状态和执行人。
   - 运营电脑运行本地 worker，从云端拉取任务，调用现有本机 Node 爬虫和浏览器环境。
   - 本地只上传爬取结果、引用、失败原因和日志摘要，不上传 AI 平台账号、密码、Cookie 或浏览器登录态。
   - 第一版先做手动启动脚本，不做开机自启、远程控制浏览器和复杂多机调度。
4. 引用情报分析正式流程已完成：
   - 页面点击 `生成引用情报` 后会跑正文抓取、阶段一结构抽取、阶段二结构归并、阶段三插件生成。
   - 页面只展示三阶段插件和对应来源文章标题/URL。
   - 内容生产已经按 `parent_type` 把引用情报插件挂到对应父类型下作为子类型使用。
5. 把当前演示链路固化成标准流程：客户配置平台 -> 问题组爬取 -> 当日整理 -> 引用/竞品洞察 -> 引用情报分析 -> 内容生成。
6. 单独诊断 qwen 零引用样本，判断是平台无引用还是解析漏提取。
7. 如果运营认可“优质竞品宣传文章”展示，再考虑做正文抓取、正文摘要、人工标注和长期沉淀。
8. 内容生产下一步先补“审核/版本对比/事实校验”中的最小闭环，不要直接上复杂记忆系统。

## 12. 长期计划

### 第一阶段：高频引用文章整理与正文结构分析

目标：先把当日资料整理里的高频引用文章从“Top 列表”升级成“可指导内容生产的样例分析”。当日数据整理负责统计高频引用文章；引用情报分析负责在此基础上抓取正文、抽取结构、合并结构并生成内容生产插件。

优先分析：

- 高频引用文章的标题结构。
- 正文信息密度。
- 用户决策维度。
- 价格说明、适合人群、避坑提醒等模块。
- 多篇 Top 文章之间是否只是标题不同、信息重复。
- 哪些文章值得作为样例，哪些只是同质化变体。
- 我们自己的客户资料或历史内容缺少哪些信息。

这一阶段先输出结构建议和可复用写法，交给运营确认，不要直接自动生成大批文章。

当前已落地的引用情报正式流程：

1. 传统代码从当日全平台高频引用候选中取 URL，目标抓取 20 篇可用正文；正文失败直接丢弃并后补。
2. 阶段一 LLM 只看单篇标题和正文，输出父类型、开头结构、正文模块和结尾写法。
3. 阶段二 LLM 只看阶段一结构摘要，负责合并相似写法并命名子类型。
4. 阶段三 LLM 只看阶段二结构簇，生成可插入内容生产的插件。

后续如果继续扩展，可以再做轻量 LLM 判断程序，对 Top 引用文章打标签：文章类型、信息价值、是否重复、可借鉴点和不建议借鉴点。

### 第二阶段：证据层稳定

目标：爬取、入库、任务报告、错误诊断可靠。

必须保证每条原始记录至少包含：

- `client_id`
- `group_id`
- `question`
- `answer`
- `refs`
- `source_platform`
- `brand_mentioned`
- `geo_score`
- `today`
- `crawl_time`
- 可追溯的 `task_id` / task report 信息

### 第三阶段：内部情报层

目标：让运营能从当日整理和记录库看懂“AI 引用了谁、引用了什么、哪个平台变化了”。

包括：

- 平台对比。
- 高频引用文章。
- 高频引用文章中宣传优质竞品的人工精选文章。
- 高频引用来源平台。
- 其他品牌/门店提及。
- 长期趋势。
- 异常样本追溯。

爬取记录库按 AI 平台重组展示和长期趋势放在长期做，不进入当前短期计划。

记录库后续需要按问题和引用文章重组，而不只是按日期堆记录：

- 按问题看：今天是否提及客户品牌、今天引用了哪些文章、哪些引用是新增的、哪些历史常见引用消失了、哪些问题长期不提品牌。
- 按引用文章看：首次出现时间、最近出现时间、出现在哪些问题和 AI 平台、是否持续高频、新增高频、热度下降或长期稳定。
- 高频引用文章需要标注大致类型，例如客户自有文章、竞品文章、平台文章、媒体/百科文章或其他类型。
- 高频引用文章还需要判断同质化程度，避免 Top 列表被多篇同类榜单、同类攻略或同一发布主体的变体文章占满。

### 第四阶段：引用规律与模板沉淀

这部分不放在当日整理和记录库主界面，后续作为独立模块。

包括：

- 标题规律。
- 文章类型。
- 正文结构。
- FAQ、对比表、选择标准等可复用模块。
- 客户专属内容模板。

### 第五阶段：内容生产与发布闭环

内容生产应基于：

```text
客户真实资料 + 优质样例链接 + 高频引用文章 Top20 + 运营意见 + 历史生成内容
```

当前 MVP 已经支持资料全量注入、样例链接、Top20 样例、多轮历史和 `deepseek-pro` 生成。后续正式批量生成前仍建议先输出文章生产规划表，人工确认后再生成，避免雷同和编造。

内容生产长期优化还包括：

- 历史记忆：保存标题、角度、一级/二级小标题、文章类型和人工审核意见，用于减少连续发文雷同。
- 客户知识包 / 客户 skill：用 LLM 将 PDF 和人工资料重写整理成品牌事实、服务项目、适合人群、价格表达、地址/区域、资质、禁写风险、可写卖点、常见用户顾虑和标准回答。
- 客户知识包必须允许运营人工审核、修改和确认，不能把模型整理结果直接当事实使用。
- 后续不要优先走传统 RAG 路线，不把客户原始资料切片后靠 embedding 相似度召回；更适合把审核后的客户知识包作为高优先级上下文直接进入内容生成。
- 对比型内容可以增加“竞品资料准备”步骤：先根据当日竞品分析、问题组和客户行业列出主要竞品或机构类型，再通过联网搜索、公开官网、地图/平台资料或运营资料整理基础信息。
- 竞品资料只用于客观对比，不能自动生成攻击性结论或未经证实的排名；资料不足时应提示运营补充或谨慎表达。

长期闭环应是：

```text
爬取记录 -> 问题变化 -> 引用文章趋势 -> 内容缺口 -> 内容生成 -> 发布后再监测
```

后续还需要记录：

- 发布平台。
- 标题。
- 链接。
- 发布时间。
- 对应问题组/问题。
- 是否被 AI 引用。
- 是否提及品牌。

## 13. 常用命令

主仓库测试：

```powershell
cd C:\Users\21295\OneDrive\programing\geo_v2-pro
.\run_tests.bat
```

健康检查：

```powershell
.\health_check.bat
```

手动启动 Node bridge 模式：

```powershell
cd C:\Users\21295\OneDrive\programing\geo_v2-pro
$env:GEO_NODE_CRAWLER_PLATFORMS="all"
.\.venv\Scripts\python.exe -u app.py
```

外部 Node 爬虫测试：

```powershell
cd C:\Users\21295\OneDrive\programing\ai-search-crawler（进阶API处理）
node --test test\bridgeMode.test.js test\doubaoNonInteractive.test.js test\extractionTimeout.test.js test\responseTimeout.test.js test\yuanbaoSidebar.test.js
node --check src\index.js
node --check src\responseTimeout.js
node --check src\adapters\doubaoAdapter.js
node --check src\adapters\yuanbaoAdapter.js
```

## 14. 最近验证结果

2026-07-03 主仓库：

```text
.\run_tests.bat -> 96 tests OK
```

2026-07-02 外部 Node 爬虫：

```text
node --test test\bridgeMode.test.js test\doubaoNonInteractive.test.js test\extractionTimeout.test.js test\responseTimeout.test.js test\yuanbaoSidebar.test.js -> 12 tests OK
node --check src\index.js -> OK
node --check src\responseTimeout.js -> OK
node --check src\adapters\yuanbaoAdapter.js -> OK
```

## 15. 风险边界

低风险：

- 更新文档。
- 补单元测试。
- 修正错误提示。
- mock Node bridge 验证 Flask API 行为。
- 只读健康检查。

中风险：

- 用真实客户和问题组触发 Node bridge 爬取。
- 修改外部 Node 平台适配器。
- 修改登录态复验逻辑。
- 批量跑某个平台。

高风险：

- 大批量真实爬取多个平台。
- 删除或迁移 `data/`。
- 删除登录态文件。
- 重启或停止正在给运营使用的服务。
- 连续请求豆包绕过验证码或风控。

中高风险操作前必须说明会影响哪些文件、是否写入 `data/`、如何回退。

## 16. 给下一位 agent 的接手指令

开始工作前：

1. 先读这份文档。
2. 用 `git status --short` 看当前工作区，不要 reset 或 checkout 用户改动。
3. 如果不需要重启，只做代码和测试，不碰运行中的服务。
4. 如需确认服务状态，优先用 `.\health_check.bat`。
5. 跑 `.\run_tests.bat` 确认主仓库测试。
6. 真实爬取前确认 VPN/代理状态，尤其是豆包。
7. 当日数据整理按“当天累计数据”作为运营口径；任务批次只用于审计、异常定位和补录追溯，不作为当日整理入口。

下一步最建议先做：

```text
先部署当前提交到云端并验收；再配置 Git 部署链路，避免继续用 scp/zip 手工同步；最后按模块小步重构 app.py、templates/index.html、爬虫调度、引用情报和内容生产代码。
```
