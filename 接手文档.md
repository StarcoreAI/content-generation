# GEO Agent 接手文档

更新时间：2026-07-22

这份文档只保留当前状态、下一步优先级和风险边界。已经完成的历史交接、排障过程和旧计划不再放在这里；需要操作细节时看对应文档。

## 1. 一句话理解项目

GEO Agent 是公司内部使用的 GEO 运营工作台。它以客户为中心，围绕固定问题组向多个 AI 平台提问，保存回答、引用源、品牌/门店提及和任务报告，用于运营复盘、引用情报、记录追溯和内容生产。

当前支持的平台：

```text
DeepSeek -> 元宝 -> 千问 / Qwen -> Kimi -> 豆包
```

豆包放最后，降低登录过期、验证码或地区风控对其他平台的影响。

## 2. 当前状态

- 项目已经进入内部云端试用和维护阶段，不是从零搭建阶段。
- 第一轮可维护性重构已经收口：后端部分业务逻辑已从 `app.py` 拆到 `services/`，前端已拆成 `templates/index.html`、`static/js/app.js`、`static/css/app.css`。
- 云端部署优先走 Git：本地 commit/push 后，服务器 `/srv/geo-content-v2` 执行 `git pull --ff-only`，再重启 `geo-content-v2.service` 并验收。
- 文档不固定最新提交 hash；部署前以 `git log -1 --oneline` 和 `git status --short` 为准。
- 云端仍以 JSON 文件和少量本地文件/SQLite 混合存储为主，继续保持单 gunicorn worker + 多线程，不要为了并发直接开多进程。
- 运营本地 worker 和已发给运营的爬取包不作为当前重构目标；云端负责创建 crawl job 和接收回传数据，真实爬取由运营电脑使用本机浏览器登录态执行。
- 核心功能列表中的资料模块顺序是：引用情报分析 -> 客户资料解析 -> 竞品资料解析 -> 内容生产。客户资料、竞品资料、内容生产上传资料是三套独立系统，不共用上传目录和资料索引。
- 客户资料解析支持上传 `txt/md/pdf/doc/docx/xlsx/xls`，已完成固定 pipeline：机械抽取 -> Filter Worker -> Reducer -> Output Worker。核心文件是 `services/material_package_extractor.py`、`services/material_filter.py`、`services/material_reducer.py`、`services/material_output.py`、`services/material_pipeline.py` 和对应 `scripts/run_material_*.py`。
- 客户资料 `AI联网扩展资料` 已接入真实流程：基于 `data/material_packages/<cid>/latest_injection.md` 生成 6 个检索词，调用 Tavily Search，每个词最多保留 2 条来源，生成三类 GEO 素材 Markdown，并保存到 `data/material_packages/<cid>/latest_web_supplement.md`。它默认不合并回主资料包。
- 竞品资料解析已作为独立模块接入：支持上传竞品资料、按真实竞品名称合并规整；联网搜索先由一次检索词 LLM 按客户资料中的行业/业务场景生成分竞品、多角度检索词，可选上传竞品资料只补同名竞品的主体、地区和服务线索；资料缺失或生成无效时退回“名称+行业/品类、怎么样/靠谱、简介”三条固定词。每竞品最多保留 8 条去重来源，再串行整理一次；长正文不足 3 条时才补入该竞品全部短摘要。普通“联网搜索”会用本次名单的成功结果整体覆盖联网 Markdown/来源 JSON；前端单竞品“重新搜索”仍只替换该分节，失败项或仅标题空壳不写入。每次联网搜索额外覆盖写入 `latest_web_run_report.json`，记录检索词模型输出、逐查询原始结果与过滤数量、最终选中来源、整理模型输出和失败阶段，供排障下载。竞品联网资料正文使用陈述句，不输出链接、URL、来源标签或泛化比较指导小节。
- RWMeiti 分发目前只合入后端基础能力：可用环境变量配置供应商、同步自媒体资源并创建人工触发的订单；分发管理前端仍在工作区、尚未提交或部署。未完成供应商签名向量核验、订单状态回查和发布 URL 登记前，禁止配置真实凭据或发起真实下单。
- 内容生产使用独立的内容生产资料库：上传后会即时解析，生成文章时读取 `data/content_uploads` / `data/content_materials_index.json` 下已确认的内容生产资料；客户资料解析模块产出的 `latest_injection.md` 和 `latest_web_supplement.md` 通过内容生产页显式开关选择是否使用。
- 内容生产已切换为统一新链路：选择输入 →（必要时）角度/FAQ 懒生成 → 写法库抽样 → 简报 LLM → 写作 LLM → 质量门禁 → SQLite 入库。入口是 `app.run_content_generation(...)`，网页单篇、批量 job、CLI 均复用它；不要重新拼装生成链路。简报返回无效 JSON 时会在同一篇内重试一次；每次简报均覆盖写入 `data/content_generation_diagnostics/<cid>/latest_planning_brief.json`，按本次单篇或批次记录模型名、`finish_reason`、响应长度、截断响应、解析状态与失败摘要，不记录提示词或密钥。
- 内容生产的简报/写作 prompt 已补“展开来源”和 GEO 宣传点指引：宣传点只写主题、指向资料小节和展开角度，写作从指向原文取材、不得照抄宣传点原句；品牌节围绕 2-4 个宣传点展开，背景类章节压为锚点短段，“行业公共背景”不得作为品牌节展开来源。内容生产提示词不设明确字数限制。常规服务事实用陈述句，品牌自述数字和强主张才保留来源话术。该字段兼容旧简报，仍为可选。
- 内容生产只读取人工转正的写法库 `active` 条目，并把抽样、简报、实际竞品子集和资料来源记录在文章 `provenance`；文章对象同时保存 `brief`、`gate_report`、版本/批次关联字段。
- 人群角度、FAQ、竞品规则都是内容生产页的“选择题”，而非客户管理页：角度/FAQ 可启停、增删，空配置首次生成时可懒生成并立即落库（AI 来源、默认启用）；全部手动停用不触发懒生成。竞品规则只从 Markdown 的 `##` 二级标题识别真实机构名称，支持必须用/禁止用/随机池；`###` 及更深的“基本信息”“业务范围”“价格线索”等栏目标题绝不能当成机构。
- 对比型提示词要求使用本篇实际选择的真实竞品名称，不能用 A/B/C 代替；客户品牌永远置于竞品前，不拉踩、不排名、不复读竞品强主张。
- 新引用情报旁路已完成真实验证：确定性预检、stage0 归并/定性、stage1 解剖卡、stage2 入库比对均有独立服务、脚本和测试。2026-07-17 翼升学批次产出 35 个 `industry:成人教育` 写法库候选条目（7 骨架、19 模块、9 引用友好清单），所有 AI 农场风险来源均停留在候选区。
- 写法库已有 API，并已归入“引用情报分析”页：可按 scope 浏览条目、来源、风险和最近入库动态，并手动转正、退役或恢复候选。内容生产已经只读取 `active` 条目。
- 引用情报正式 job 已切换到新链路：抓取 -> stage0 过滤定性 -> stage1 解剖卡 -> stage2 入库比对。旧 `reference_stage1.py`、`reference_stage2.py`、`reference_stage3.py`、历史插件读取兼容层、插件 API 与前端卡片均已删除；代码中不应再新增 plugin/插件 概念。
- 质量门禁已上线并在写作完成、入库之前执行；被拦截的文章仍照常入库，`gate_report_json` 留痕，前端“质量门禁”列表展示全部文章（不筛掉失败项）。代码层拦截、LLM 层失败均不会让写作失败的半条记录落库；LLM 层是 fail-open。
- 批量生成 v1 已上线：前端固定 1/3/5 篇，默认 5；1 篇保持原同步接口，3/5 篇使用内存 job 且单 worker 内严格串行，支持轮询与终止。每篇有同一 `batch_id`，单篇失败不影响后续篇，门禁拦截记为“门禁拦截”但仍入库。
- 已完成多租户收口：运营账户只能访问自己名下客户及内容，越权资源统一 404；admin 全量访问。行业/global 写法库对运营可读但仅 admin 可写。
- 本地 worker 同平台并行窗口已接入试用方案；遇到登录态、验证码、风控或高峰拒绝时，仍按运营说明执行补登录、取消任务、单平台补爬。
- 爬取记录库和当日数据整理里的旧“深度分析”入口、后端接口和 `services/deep_analysis.py` 已删除；后续不要再按这个旧功能设计入口。

## 3. 文档分工

- `接手文档.md`：当前状态、下一步优先级、风险边界。
- `工程化说明.md`：本地环境、启动、测试、部署、数据文件、worker 和运行约束。
- `运营使用说明.md`：运营同学如何 setup、启动本地 worker、停止爬虫和导出诊断日志。
- `docs/engineering-rules.md`：入口脚本编码、运营 worker、云端部署和数据补录的硬约束。
- `docs/content-plan.md`：内容生成规则、资料来源边界、引用情报如何接入内容生产、短期和长期内容路线。
- `docs/content-refactor-short-term.md`：内容链路重构的当前施工文档，顶部有已完成进度快照，第 6 节是剩余施工清单。**做内容链路相关开发前必读。**
- `docs/content-refactor-long-term.md`：重构之后的方向（分发、观测归因、平台微调、记忆与自进化），只写想法和目标。
- `docs/content-refactor-examples.md`：三个模块产出的人工标准答案全文（引用情报分析、联网扩展、策划简报），验收时对照，可做测试 fixture。

## 4. 当前主流程

标准运营链路：

```text
客户配置平台 -> 问题组爬取 -> 当日整理 -> 品牌/竞品/门店提及 -> 引用情报分析 -> 客户资料解析 -> 竞品资料解析 -> 内容生产
```

爬取链路：

1. 云端页面在问题组里创建 crawl job。
2. 运营电脑启动本地 worker 领取任务。
3. 本地 worker 调用外部 Node 爬虫和本机浏览器登录态执行真实平台提问。
4. 本地 worker 只回传回答、引用、失败原因和日志摘要。
5. 云端写入原始记录、任务报告和当日整理数据。

不要把 AI 平台账号、密码、Cookie、Playwright storage state 或浏览器登录态上传到云端。

## 5. 当前能力边界

### 当日数据整理

定位：当前客户当天数据的内部情报汇总。

应展示：

- 当天记录数、引用数、品牌提及率、平台完成情况。
- AI 平台对比：各平台记录数、引用数、品牌提及数、异常数。
- 其他品牌/门店提及统计。
- 高频引用文章。
- 高频引用来源平台。
- 异常数据：零引用、空回答、登录异常、疑似卡住、测试/补爬数据混入。

当日整理按“当天累计数据”作为运营口径；任务批次只用于审计、异常定位和补录追溯，不作为当日整理默认入口。

### 爬取记录库

定位：当前客户的长期数据追溯和审计库。

应展示：

- 按 AI 平台分类查看长期记录。
- 按问题组、日期范围筛选。
- 选择已有问题后，按 AI 平台查看逐日品牌提及（同日多轮任一提及即记提及，并保留该格记录数）。
- 按日期查看引用文章池的当日新进和留存；留存项显示首次被引日期、自然日留存天数、当日/累计被引次数和涉及 AI 平台。
- 按最近 12 个 ISO 周查看引用来源站份额；展示累计引用 Top10 域名及“其他”桶，URL 缺失或无效时按来源站名归类。
- 单条完整证据：问题、AI 平台、完整回答、引用文章、提到的品牌、爬取时间。
- 数据审计：错误记录删除、导出、定位某次任务原始数据。

### 品牌/竞品/门店提及

暂时不维护竞品名单。客户行业变化大，前期维护成本高。

当前处理方式：

- 使用 LLM 从回答正文里抽取被推荐或被提及的门店、品牌、公司名。
- 不强行排除本客户品牌；自有品牌和竞品一起作为“被 AI 提到的实体”展示。
- 抽取结果写入原始记录的 `mentioned_entities`。
- 爬取入库后不自动跑实体识别；当日整理页提供“生成竞品提及”按钮，按当前客户、日期、平台、问题组和批次筛选范围手动排队生成。
- 手动生成只处理 `mentioned_entities` 为空的记录，重复点击不会对已识别记录重复叠加。
- 页面允许人工删除当前筛选范围内的错误实体识别结果，不删除原始回答。

推荐结构：

```json
{
  "entities": [
    {
      "name": "某某汽车音响",
      "type": "门店",
      "sentiment": "positive",
      "evidence": "回答中的短片段"
    }
  ]
}
```

### 引用情报分析

新目标：把当日高频引用文章沉淀为可审核、可积累的写法库条目。线上正式流程是：从当前客户当天全平台高频引用候选中取 URL，目标抓取 20 篇可用正文 -> 确定性预检与 stage0 过滤/一稿多发归并 -> stage1 解剖卡 -> stage2 入库比对。运营在“引用情报分析”页的写法沉淀分区审核候选条目。

引用情报页展示写法沉淀的骨架、段落模式、引用友好清单、来源与风险；已无历史插件卡片、插件接口或内容生产兼容读取。

### 内容生产

当前内容生产以两类父类型为主：

- `对比型`：用于“哪家好、推荐、排名、全攻略、怎么选”等内容。
- `介绍型`：用于品牌/服务介绍。

内容生产已由抽样/简报层读取人工转正的写法库 `active` 条目，不再使用历史插件。实际链路为：

```text
内容生产页选择（资料开关、角度、FAQ、竞品规则）
  -> 空配置时一次性懒生成角度/FAQ
  -> 本篇竞品子集（必须用 + 随机池，排除禁止用）
  -> 写法库抽样（批内避开骨架+开头组合）
  -> 简报 LLM -> 写作 LLM -> 质量门禁（代码层 + LLM 层）
  -> SQLite 内容文章与溯源/报告入库
```

内容生成必须基于内容生产资料、客户资料包、联网扩展、简报与写法库，不能编造客户事实、价格、资质、案例或评价。成文禁止出现“竞品”“客户资料”“资料未提供”等内部流程词；缺信息时应省略该维度，或转成读者的核验动作。

内容生产上传资料与客户资料解析上传资料是两套独立系统。运营在内容生产页上传的资料会即时解析并参与文章生成，可逐份关闭；客户资料解析模块产出的 `latest_injection.md`、`latest_web_supplement.md` 已有显式使用开关，运营可勾选使用或不使用。

质量门禁页是所有生成文章的运营入口：`pass` 显示“可发布”，`warn` 需人工判断，`blocked` 建议修改后再用；每个检查项有中文说明。人工编辑即时保存、不重跑门禁，保留原 AI 版门禁报告并标“人工已编辑”；AI 修改会带本链修改意见生成新版本，并重新经过门禁。质量门禁页可查看全文、复制、人工编辑和 AI 修改，不依赖先进入内容生产页。

### 客户资料整理模块

定位：把客户上传的复杂资料包整理成大模型可读、可审核、可复用的客户资料包。

当前边界：

- 不做重型 Master/Worker 动态编排框架；当前先走固定 pipeline。
- pipeline 顺序是：机械抽取 -> Filter Worker 大量过滤 -> Reducer 裁剪去重 -> Output Worker 生成完整整理资料和内容生产建议。
- Filter Worker 以整个资料包为输入，先做确定性去重，再让模型比较所有候选单元并选择核心资料和少量代表。
- 每个结果只输出一个权威状态 `{"unit_id": "...", "status": "core|representative|..."}`；只有 `core` 和 `representative` 进入下游，不再同时维护 `useful` 布尔值。
- 竞品资料不进入客户资料模块；竞品资料已经单独做成独立模块。
- 同一数据集拆分出的结构相似明细不能全部进入默认资料包；没有汇总单元时最多保留 3 个代表，其余标记为 `reference_only` 或 `redundant`。
- 本地 `pdf/` 导入入口已删除，前端和后端后续都不再维护这个功能；资料入口先只保留网页上传。
- 资料上传支持 `txt/md/pdf/doc/docx/xlsx/xls`。旧 `.doc` 解析依赖系统工具 `antiword` 或 `catdoc`；`.xls` 仍建议转成 `.xlsx` 后上传。
- `latest_injection.md` 按八个方向组织：品牌基础、产品与服务、核心优势、目标人群与需求痛点、价格与费用表达、信任凭证、合规风险表述、行业公共背景。
- `AI联网扩展资料` 面向三类 GEO 素材：品牌公开信息与优势表达角度、人群痛点与决策语境、行业公共背景锚点；不推翻客户资料，不自动合并回 `latest_injection.md`。
- 手动测试客户联网扩展可用 `scripts/run_material_web_expansion.py`。脚本支持 `--search-only`、`--sources` 只跑最后总结阶段；默认非流式，LLM 调用不传固定 `max_tokens`。

Filter Worker 当前会一次查看整个候选包：完整正文只用于本地精确去重，模型读取每个单元最多 1800 字的头、中、尾节选，按 `status` 输出核心、代表、冗余、无关或仅供参考等结果。它只决定整份单元的去留，不裁剪单元内部段落。

### 竞品资料解析模块

定位：把上传资料和公开搜索结果整理成客观竞品资料，供对比型内容参考。

当前能力：

- 前端作为独立模块放在“客户资料解析”和“内容生产”之间，不放进客户资料解析页。
- 上传资料解析复用资料包机械抽取能力，但只走大模型按竞品名称合并规整，不跑客户资料的 Filter/Reducer/Output 三阶段。
- 竞品名称必须使用真实品牌名、机构名、门店名或公司名，不能用 A/B/C、竞品1、竞品2 等占位名。
- 无法确定是否同一主体时直接分开整理，不输出“疑似同主体，待人工确认”。
- 联网搜索默认围绕约 10 个竞品：先由一次检索词 LLM 依据客户资料中的同行业业务场景生成分竞品检索词；可选竞品上传资料仅取同名分节补充主体、地区和服务线索。无资料或生成无效时，每家退回“名称+行业/品类、怎么样/靠谱、简介”三条固定词；去重后最多保留 8 条来源，长正文不足 3 条时才补入该竞品全部短摘要。普通联网搜索每次按本次名单整体覆盖结果，点名单独重搜才只替换该分节；最新一次运行的逐查询原始结果、过滤和整理状态写入同目录 `latest_web_run_report.json`。
- 页面按竞品名称合并展示上传资料和联网资料，但上传 Markdown 与联网 Markdown 可以分开下载。

当前边界：

- 不拉踩、不排名、不替客户品牌下判断；只客观整理竞品定位、业务侧重、适合人群、服务特点、限制和来源依据。
- 上传竞品资料解析中的宣传性数字和绝对化主张集中列为“宣传主张（仅记录，禁止在我方内容中复述）”，不要混入正文；竞品联网整理不再要求该集中行。
- 竞品联网资料的常规事实直接使用陈述句；官网自述、第三方、UGC、疑似投放均可作为整理素材，最终 Markdown 不输出链接、URL 或来源标签，不拉踩、不排名、不替客户品牌下判断。
- 竞品资料不自动混入客户资料包。内容生产后续如要直接读取竞品资料，应做显式选择或开关。

## 6. 当前短期优先级

当前短期计划：

1. **日常真实内容验收（由运营执行）**：用内容生产页默认 5 篇批量生成，人工检查多样性、资料吸收、内部词汇泄漏和门禁误报。不要由开发 agent 擅自跑真实 LLM 验收或大批量生成。
2. **观察并小修门禁**：重点观察事实溯源、行业禁词与警示语境、竞品强主张复读。代码层已有通用与医疗/教育/金融行业词表；外部扩展文件为 `data/quality_gate/banned_words.json`。警示/否定语境的命中降为 warn，仍交 LLM 复核。
3. **收集运营界面反馈**：重点是内容生产页的选择题、批量进度、质量门禁编辑/AI 修改、客户资料和竞品资料展示；只做针对性修补，不重建流水线。
4. **后续较大事项仍未实施**：结构化竞品实体库、完整批次管理与高级轮换/自动重抽样、文章版本可视化对比、RWMeiti 订单状态回查/发布 URL 登记与观测归因。做前先更新方案并单独提交。
5. 持续观察本地 worker 双窗口实跑效果；如果出现新的登录态或平台风控问题，只做针对性修补，不扩大爬虫能力承诺。

## 7. 长期方向

长期路线只作为方向，不代表当前马上要做。内容侧的长期规划（分发、观测归因、平台微调、记忆与自进化）详见 `docs/content-refactor-long-term.md`。

1. 证据层稳定：爬取、入库、任务报告、错误诊断可靠；每条原始记录都能追溯到 task report 和原始回答。
2. 内部情报层：让运营能从当日整理和记录库看懂 AI 引用了谁、引用了什么、哪个平台变化了。
3. 记录库重组：后续按问题和引用文章看长期变化，而不只是按日期堆记录。
4. 引用规律与模板沉淀：标题规律、文章类型、正文结构、FAQ、对比表、选择标准、客户专属模板。
5. 内容生产与发布闭环：记录发布平台、标题、链接、发布时间、对应问题、是否被 AI 引用、是否提及品牌。

## 8. 暂时不要做

- 不要为了“更干净”继续大规模重构业务逻辑。
- 不要在云端直接改代码；先本地修、测试、commit、push，再让云端拉取同一个提交。
- 不要把云端从单 worker 直接改成多进程。
- 不要整文件覆盖云端 `data/raw_records.json`。
- 不要上传平台 Cookie、storage state、API Key 或客户真实数据到公开仓库。
- 不要在没有运营判断前做优质文章自动评分模型。
- 不要马上上复杂 RAG、复杂记忆系统、复杂权限矩阵、多平台发布或无人值守稳定爬虫。
- 不要在客户资料模块里混入竞品资料；竞品资料保持独立模块。
- 不要绕过内容生产页的显式开关把客户资料包强行注入生成 prompt；运营必须能选择使用或不使用 `latest_injection.md`、`latest_web_supplement.md`。
- 不要先做 Master/Worker 动态资料编排框架；当前用 pipeline 先跑通。
- 不要为了演示反复大规模爬虫，尤其不要连续请求豆包绕过验证码或风控。

## 9. 数据同步和补录原则

代码同步和数据同步分开处理。

- 代码部署走 Git；Git 不可用时才用 `scripts/deploy_cloud_package.sh`。
- 云端 `data/`、`pdf/`、`logs/`、`.env` 必须保留或备份。
- 本地 `raw_records.json` 有记录缺失在云端时，不要整文件覆盖。
- 缺记录时使用 `scripts/import_missing_raw_records.py`，先 dry-run 看 `append_count` 和 `duplicate_count`，确认后再 `--apply`。
- 真正 `--apply` 前建议短暂停止 `geo-content-v2.service`，避免云端同时写 `raw_records.json`。

## 10. 风险边界

低风险：

- 更新文档。
- 补单元测试。
- 修正错误提示。
- mock Node bridge 验证 Flask API 行为。
- 只读健康检查。

中风险：

- 用真实客户和问题组触发 Node bridge 爬取。
- 修改外部 Node 平台适配器。
- 修改登录态复验逻辑。
- 批量跑某个平台。

高风险：

- 大批量真实爬取多个平台。
- 删除或迁移 `data/`。
- 删除登录态文件。
- 重启或停止正在给运营使用的服务。
- 连续请求豆包绕过验证码或风控。

中高风险操作前必须说明会影响哪些文件、是否写入 `data/`、如何回退。

## 11. 新 agent 开始工作前

1. **本次交接仅理解，不执行任务。** 先完整阅读本文件、`docs/content-refactor-short-term.md` 的“当前进度快照/剩余施工”、`工程化说明.md`、`docs/engineering-rules.md`；然后只报告其对架构、硬约束、当前未提交改动与下一步的理解，等待用户派发任务。
2. 用 `git status --short` 看当前工作区，不要 reset、checkout、清理 untracked 文件或覆盖用户改动。2026-07-21 收尾时，质量门禁/内容生产页的竞品名称过滤与选择题位置调整仍可能是未提交改动；以实际 `git status` 为准。
3. 用 `git log -1 --oneline` 确认当前提交，不要相信文档里的旧 hash。已推送的最近两次功能提交是 `3f8fea6`（新内容链路/门禁/批量/隔离等）与 `043dbb9`（行业禁词）。
4. 如果不需要重启，只做代码和测试，不碰运行中的服务。模板或 Python 服务改动不会自动反映在当前非热重载服务中；重启必须先获得用户授权。
5. 如需确认服务状态，优先用 `.\health_check.bat`。代码改动后跑相关测试；主仓库完整验证用 `.\run_tests.bat`。
6. 不执行真实 LLM 验收、真实爬取、批量生成、数据迁移、服务重启或云端部署，除非用户明确授权；任何涉及 `data/`、登录态、API Key、云端服务重启的操作，先说明影响范围和回退方式。

## 12. 常用命令

主仓库测试：

```powershell
.\run_tests.bat
```

健康检查：

```powershell
.\health_check.bat
```

本机启动：

```powershell
.\run_dev.bat
```

手动启动 Node bridge 模式：

```powershell
$env:GEO_NODE_CRAWLER_PLATFORMS="all"
.\.venv\Scripts\python.exe -u app.py
```
