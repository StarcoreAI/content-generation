# GEO Agent 接手文档

更新时间：2026-08-02

GEO Agent 是内部 GEO 运营工作台：运营按客户的问题组在合同 AI 平台采集回答与引用，复盘记录，沉淀客户/竞品资料和行业写法路线，并生成可人工审核的内容。

## 当前产品边界

- 客户、竞品、内容上传资料彼此独立。内容生产只在运营显式选择时读取客户资料和竞品分节，绝不自动注入。
- 内容生产统一入口为 `app.run_content_generation(...)`：按介绍型或对比型抽取行业路线与所选资料，使用**单阶段写作**生成，质量门禁只提醒后入 SQLite。
- 介绍型需要客户资料；对比型需要客户资料和至少两家运营明确选择的竞品。竞品不随机抽取。
- 竞品知识库已完成：每客户一份、以真实竞品名称 `##` 分节。当日整理可从高频引用文章补充资料；竞品删除只删除当前章节，后续来源同步可恢复。
- 记录库的问题组趋势和逐题矩阵只展示运营选定的一个合同 AI 平台；不提供全平台汇总矩阵。选择层临时报告展示已移除，保留 Query 场景词提示。
- 引用情报分析可分析单题或整个问题组，默认整个问题组；一次只选择一个合同 AI 平台。
- 写法库按行业、介绍型/对比型区分；路线直接可用，创建和删除即入库。
- 长期平台偏好、经验库、自进化、自动权重优化、RAG 等方向不再排期。

## 当前资料与知识库

- 客户资料、竞品资料、Query 场景词、行业写法库可导出为 DOCX；质量门禁和系统提示词不提供下载。
- 资料编辑采用自动保存：输入防抖、失焦立即保存、增删改名立即保存；失败时可重试。
- 系统提示词目录只读，展示经审核的静态模板与变量说明；不得泄露最终请求、客户正文、抓取原文或密钥。

## 工程约束

- 客户隔离是硬约束，越权访问返回 404。
- 不修改爬虫、本地 worker、平台账号登录态或远程部署，除非用户明确要求。
- 修改前写失败测试，修改后运行相关测试和 `git diff --check`。工作区可能有用户遗留改动，只处理当前任务涉及的文件。
- 云端部署、`git pull` 与服务重启由用户执行。

## 优先阅读

1. `docs/content-plan.md`
2. `docs/knowledge-base-direction.md`
3. `docs/citation-selection-findings.md`
4. `app.py`、`services/content_route_generation.py`、`services/knowledge_base.py` 与对应测试

历史 `docs/superpowers/plans/` 仅用于审计，不能当作当前产品说明或待办。
