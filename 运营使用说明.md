# GEO 运营使用说明

这份说明按“电脑里什么环境都没有”的情况写。运营同学只需要按顺序双击文件，不需要手动设置环境变量。

## 一、收到压缩包后怎么摆放

收到 `GEO-operator-worker-windows-*.zip` 后，先右键解压。解压后应该看到两个同级文件夹：

- `geo_v2-pro`
- `ai-search-crawler`

示例：

```text
GEO-operator-worker-windows
├─ geo_v2-pro
└─ ai-search-crawler
```

不要只复制其中一个文件夹。本地爬虫需要两个文件夹配合使用。

## 二、第一次使用：Windows 首次配置

进入 `geo_v2-pro` 文件夹，双击：

```text
setup_operator_windows.bat
```

也可以双击中文入口：

```text
运营首次配置.bat
```

这个脚本会自动检查并尽量补齐：

- Python
- Node.js
- 本地 `ai-search-crawler...` 路径
- 压缩包内置的 Node 爬虫依赖
- 压缩包内置的 Playwright Chromium 浏览器
- 本地登录态保存目录

新版运营包已经带好 `node_modules` 和 Playwright Chromium，不会在运营电脑上现场执行 `npm install`、`npx playwright install` 或下载 Chrome。

如果公司电脑没有管理员权限、没有网络，或者系统没有 `winget`，脚本可能无法自动安装 Python/Node。此时把窗口里的错误截图或诊断日志发给技术同学，不要自己改环境变量。

首次配置完成后，系统会自动进入首次登录流程，一个平台一个平台打开浏览器。运营只需要在浏览器里完成登录，系统检测到登录完成后会自动进入下一个平台，不需要回控制台按回车。

## 三、每天启动本地爬虫

进入 `geo_v2-pro` 文件夹，双击：

```text
启动本地爬虫worker.bat
```

脚本会要求输入云端账号和密码：

```text
Cloud username:
Cloud password:
```

每次启动都会重新输入账号密码，这是正常的。

之后脚本会自动检查本地爬虫路径、云端连接和各平台登录状态。如果明确识别到某个平台未登录，会停在浏览器页面等待登录完成；如果只是暂时无法可靠确认登录态，则会继续启动 worker。

如果后续云端页面提示某个平台需要补登录，在云端页面点击对应平台的登录按钮。本地 worker 会领取补登录任务，并在运营电脑上打开浏览器窗口。

基础自检通过后，会出现一个本地控制小窗口，里面有：

```text
Stop local crawler
```

这个按钮用于停止本机正在跑的爬虫。

## 四、云端页面怎么配合使用

日常工作主要在云端页面操作：

1. 打开云端页面。
2. 选择客户和问题组。
3. 在问题组里点击 `一键创建爬取任务`。
4. 本地 `启动本地爬虫worker.bat` 会自动领取云端任务。

这个按钮会自动创建本地 worker 任务，不需要再找“交给本地 worker”入口。

运营不需要在本地选择平台。默认会准备所有平台，但本地 worker 只会领取云端页面已经创建的任务。

问题组可以批量添加问题。把多行问题直接粘贴进去即可，例如：

```text
1.客户适合选择哪类服务？
2.预算不同应该怎么选？
3.附近服务便利性怎么判断？
```

没有换行时，也会按一条问题正常添加。

## 五、引用情报分析怎么用

当天爬取数据入库后，进入云端页面的“引用情报分析”，选择日期后点击右上角：

```text
生成引用情报
```

页面会显示进度条。最近一次真实测试中，前面正文抓取大约 90 秒到 30%，后面会继续平滑推进。进度条只表示任务还在跑，不需要运营判断每个阶段在做什么。

如果长时间停住，可以点击：

```text
终止生成
```

完成后，页面只展示可用于内容生产的插件，包括父类型、子类型、写作要求、示例片段，以及“基于哪些文章合并”的文章标题和 URL。页面不会展示引用次数和 AI 平台。

生成出的插件会出现在内容生产对应父类型下面作为子类型。`对比型` 插件只会挂到对比型下，`介绍型` 插件只会挂到介绍型下。

如果同一客户同一天已经有人点过生成，并且任务还在运行，系统会复用同一个后台任务，不需要重复点击。

## 六、遇到反爬、排队、高峰拒绝怎么办

如果豆包、Kimi 或其他平台出现反爬、验证、服务繁忙、高峰期拒绝，这是正常情况。

处理流程：

1. 先到云端页面，取消对应平台的任务。
2. 再回到本地控制小窗口，点击 `Stop local crawler`。
3. 等一段时间。
4. 在云端页面只重新创建需要补爬的平台任务。
5. 再双击 `启动本地爬虫worker.bat`。

云端页面的“取消”用于阻止任务继续入库。本地 `Stop local crawler` 用于停止本机浏览器和爬虫进程。两个动作分别解决不同问题。

## 七、如果控制窗口关掉了

可以双击：

```text
停止本地爬虫worker.bat
```

它会停止本机正在跑的 worker、Node 爬虫和 Playwright 浏览器。

## 八、常见问题

### 提示找不到 Node crawler folder

说明 `geo_v2-pro` 和 `ai-search-crawler...` 没有放在同一个上级目录下。

处理方式：把两个文件夹放到一起，再重新双击 `setup_operator_windows.bat`。

### 提示 Python was not found 或 Node.js was not found

先双击 `setup_operator_windows.bat`。

如果 setup 仍然失败，把错误截图发给技术同学。常见原因是公司电脑没有管理员权限、没有网络，或者系统策略禁止自动安装。

### 平台登录后又提示未登录

重新运行 `启动本地爬虫worker.bat`，按浏览器提示再登录一次。不要手动删除 `storage` 或 `data` 目录。

### 某个平台一直卡住

按第五部分处理：云端页面取消任务，本地点击 `Stop local crawler`，过一段时间只补爬这个平台。

### setup 提示压缩包不完整或启动失败

新版运营包不会在 setup 阶段下载 Chrome。如果提示 `Operator package is incomplete`、缺少 `node_modules`、缺少 `ms-playwright` 或缺少 `chrome-win64\chrome.exe`，说明收到的压缩包不完整、解压被安全软件拦截，或者只复制了其中一个文件夹。

处理方式：重新解压最新的 `GEO-operator-worker-windows-*.zip`，确保 `geo_v2-pro` 和 `ai-search-crawler` 两个文件夹在同一个目录下。仍然失败时，进入 `geo_v2-pro` 文件夹，双击 `导出诊断日志.bat`，把生成的 zip 发给技术同学。

## 九、Mac 同学怎么用

Mac 同学目前可以只访问云端网页，创建任务、查看结果、取消任务都可以。

Mac 暂时不要跑本地 worker。当前本地启动、停止、自动配置脚本是 Windows 版本。如果 Mac 同学也需要承担本地爬虫，需要技术同学再提供 macOS 专用脚本。
## 十、出问题时怎么发日志

所有运营入口都会自动把窗口内容保存到 `geo_v2-pro\operator_logs`。
日志默认保留 14 天，过期日志会在下次启动脚本时自动删除。

如果技术同学需要排查问题，运营只需要进入 `geo_v2-pro` 文件夹，双击：

```text
导出诊断日志.bat
```

脚本会在 `operator_logs` 里生成一个 `GEO-diagnostic-时间.zip` 文件。把这个 zip 发给技术同学即可。
诊断包只包含最近日志、Python/Node/npm 版本、本地爬虫路径和依赖检测结果，不包含平台登录态和密码。
