# GEO 系统模块说明

更新时间：2026-08-04

GEO 用于按客户问题组观察 AI 平台回答、实际引用文章和品牌提及情况，并在运营明确选择资料的前提下辅助内容生产。本说明按模块描述系统已经具备的功能、产生的数据和使用边界；运营可以据此理解页面功能，维护工程师也可以据此了解系统的实际链路。

```text
客户管理 → 问题组 → 云端爬取任务 → 本地 worker 执行 → 原始记录
                                              ↓
当日数据整理 / 爬取记录库 ←───────────────────┘
        ↓                         ↓
竞品资料补充                 引用情报分析
        ↓                         ↓
竞品知识库 ← 客户资料 / 场景词 / 行业写法库 → 内容生产 → 质量门禁 → 发布
```

## 0. 本地爬虫 worker

### 模块作用

云端页面负责创建、分发和展示爬取任务；运营电脑上的本地 worker 才会使用已登录浏览器访问 DeepSeek、元宝、千问、Kimi、豆包等平台，并把回答和引用结果回传云端。平台登录态只保留在运营电脑，不上传到 Git。

### 运营怎么使用

日常使用时，运营在云端创建好爬取任务后双击 `启动本地爬虫worker.bat`，输入云端账号和密码，worker 会自动领取已经创建的任务，并自动弹出本地控制窗口。需要正常停止时使用 `停止本地爬虫worker.bat`；如果控制窗口关闭或卡住，也使用该文件停止 worker。平台登录状态失效而浏览器自动打开对应平台的登录页时，直接在该页重新登录即可，worker 会在登录成功后重试当前任务一次；如果没有自动打开登录页，可在云端“问题组管理”展开问题组后，点击对应平台的“平台重新登录”按钮。该按钮会创建补登录任务，本地 worker 必须保持运行才能接收并打开登录窗口；重新登录完成后，再为受影响的平台补爬原任务。

### 数据与工程说明

Windows 首次收到 `GEO-operator-worker-windows-*.zip` 后，必须保持 `geo_v2-pro` 与 `ai-search-crawler` 两个文件夹同级，再双击 `setup_operator_windows.bat` 或 `运营首次配置.bat` 完成环境检查和首次平台登录。首次配置会依次打开各平台：登录完成后不要手动关闭浏览器，应等待终端出现 `login state saved` 和 `login ready`，浏览器随后自动关闭并打开下一平台；手动关闭会使当前平台的登录态来不及保存。如果某个平台失败，可以重新运行首次配置，已经保存成功的平台会自动通过。Mac 仅支持 Apple Silicon 的 `GEO-operator-worker-macos-arm64-*.zip`，首次使用运行 `setup_operator_mac.command`；如果 macOS 提示“已损坏”，先在“系统设置 → 隐私与安全性”选择“仍要打开”，若没有该选项，只对来源可信的公司压缩包在终端执行 `xattr -dr com.apple.quarantine <完整解压目录>`，再用 `bash <setup_operator_mac.command 路径>` 启动。云端任务状态保存于 `data/crawl_jobs.json`，外部 Node 爬虫桥接在 `services/node_crawler_bridge.py`，任务报告在 `data/tasks/`。未被 worker 领取的普通爬取任务会在创建约 2 分钟后过期；登录任务不受该过期规则影响。每个平台的一次爬取任务会先在本地收集本批全部问题的结果，再一次性提交云端；云端只在收到完成任务的提交后，才将其中每条成功结果写入 `data/raw_records.json` 和 `data/raw/<client_id>/<date>.json`。因此如果浏览器、worker、电脑或网络在提交前中断，即使前面部分问题已经在本地爬到，结果也可能还没有入库而丢失，需要重新创建该平台任务补爬。worker 会对提交做有限次数重试，但不把未成功提交的结果作为可长期恢复的云端队列；任务回传在写入前会清除 Cookie、密码、Token 等敏感字段。本地 `storage`、`state.json`、Cookie 和登录态属于敏感数据，不能删除、上传、发送或提交。平台反爬、验证、高峰拒绝和临时限流是外部平台行为，正确处理是先在云端页面取消对应平台任务，再停止本地 worker，稍后只补爬受影响的平台，而不是连续重复提交。

## 1. 客户管理

### 模块作用

客户管理维护多客户的基本资料和隔离边界。每个客户都拥有唯一 `client_id`，问题组、爬取记录、知识库、内容生成和导出均以该 ID 隔离。

### 运营怎么使用

运营可在这里新增客户，并填写名称、品牌、行业和合同 AI 平台；后续可以修改这些基础信息。合同平台决定问题组页面能够创建哪些平台的爬取任务。删除客户只应发生在确认废弃后，因为它会影响该客户已经积累的运营数据。

### 数据与工程说明

客户主数据存放于 `data/clients.json`，客户级读取、编辑和导出都必须经过权限校验。删除客户会移除该客户的问题组、探测项、原始记录和旧文章数据，因此删除前应先导出或确认历史数据不再需要；部分按客户存放的资料目录可能仍作为孤立文件留在服务器上，所以“删除客户”不能当作敏感数据清理手段。同名客户会使基于名称的导出出现歧义，研究导出时应优先使用 `client_id`。

## 2. 问题组管理

### 模块作用

问题组管理维护每个客户要观察的真实读者问题，并按问题组创建平台爬取任务。问题组是爬取、引用情报和内容生产中连接 Query 的基础对象。

### 运营怎么使用

运营可以新建、编辑或删除问题组，也可以一次粘贴多行问题。根据客户已配置的合同平台创建爬取任务后，本地 worker 会自动领取任务，运营不需要在本地 worker 中再次选择平台或手动导入任务。问题应尽量写成读者会真实向 AI 提出的完整问题。

### 数据与工程说明

问题组数据位于 `data/probe_groups.json`，每条爬取记录会保留 `group_id` 和实际 `question`。修改问题组不会改写已经产生的历史记录，历史问题文本仍可用于研究和追溯；删除客户时，该客户的问题组会一并删除。

## 3. 当日数据整理

### 模块作用

当日数据整理按“客户、AI 平台和日期”查看当天实际爬取结果，是运营确认爬取完成、查看引用来源和补充竞品资料的工作台。

### 运营怎么使用

运营选择客户、单个平台和日期后，可以查看当天各问题的回答、品牌提及和引用来源。页面会按 AI 平台分别展示高频引用来源和高频引用文章，方便比较同一天内不同平台实际引用了什么。日期下拉会标出当前客户和平台实际已有爬取记录的日期，也仍允许手动选择日期。当需要补充竞品资料时，可以发起“竞品资料补充”，系统会从当前范围内被高频引用的文章提取竞品信息并写入竞品知识库；页面显示处理中时不要连续点击，如提示会覆盖人工编辑内容，应确认后再继续。

### 数据与工程说明

页面主要读取 `data/raw_records.json` 中的 `today`、`source_platform`、`question`、`answer` 和 `refs` 等字段。竞品资料补充是后台任务，前端通过轮询显示状态，重复点击不会加速任务。当前的单条、批量和按日删除只会改写 `data/raw_records.json`，不会反向修改 `data/raw/<client_id>/<date>.json` 归档；页面查询以 `raw_records.json` 为准。该模块只整理当天范围，不能替代爬取记录库中的全量历史趋势分析。

## 4. 爬取记录库

### 模块作用

爬取记录库用于复盘历史爬取结果，观察品牌提及、引用来源、问题趋势和不同问题下的文章分布。

### 运营怎么使用

运营先选择问题组和一个 AI 平台，再查看该问题组在该平台中的品牌提及率变化和逐题提及矩阵；矩阵中的每个单元格表示该题在当天实际爬取中提及品牌的次数与总次数。引用来源站变化暂时汇总该客户所有 AI 平台的数据，用于观察最近实际采集日中各来源站的引用份额。需要看某一道具体问题引用了哪些文章时，可在“按具体问题看引用文章”中选择该题；引用文章池则可按日期查看当天新增了哪些被引用文章，以及哪些文章从更早日期开始持续留存。问题组场景词推荐会根据 Query 和对应的高频引用文章，由大模型推测豆包在检索引用文章时可能使用的关键词，只作为运营理解检索场景的提示。

### 数据与工程说明

提及率变化和逐题矩阵不再提供混合的全平台总口径，只展示当前选择的单个平台，以避免不同平台的引用口径相互干扰；引用来源站变化和引用文章池则按当前实现汇总客户全部 AI 平台的数据。场景词并非平台真实公开的检索词，也不是客户或竞品事实，而是模型根据 Query 与高频文章表面信息作出的推测，持久化在 `data/selection_evidence/<client_id>/query_scenes.json`。记录库只用于观察和复盘，不会自动修改客户资料、竞品资料、写法路线或生成文章。原始记录是研究高频与低频引用文章的基础，任何同步都不应以整文件覆盖 `data/raw_records.json`。

## 5. 引用情报分析

### 模块作用

引用情报分析从实际引用文章中提取可复用的写作组织方式，并沉淀为介绍型或对比型的行业写法路线。

### 运营怎么使用

运营在此选择客户、问题组和一个 AI 平台。分析范围默认是整个问题组，也可改选其中一道具体问题；任务提交后，系统会在所选平台的实际爬取记录中，为每道 Query 汇总被引用文章，优先选取引用最多的一篇，并从其后的高频文章中再选一篇作为补充，随后抓取正文、判断文章属于介绍型还是对比型，并提炼可复用的组织方式。当前每道 Query 最多分析两篇文章：最高引用文章固定保留，另一篇从引用排名第 2 至第 5 的文章中按引用次数加权选出，用来补充有限的写法差异。之所以逐题找高频引用文章，是因为内容生产与 Query 强绑定，文章需要围绕本次 Query 的读者问题和判断主线展开，不能把不同问题下的写法直接混在一起。完成后，系统会把相近的结果合并成精简的行业写法路线，运营可到行业写法库查看和维护；最终生成文章时，路线用于确定判断主线和组织节奏，路线未覆盖的解释、过渡和展开由模型围绕本次 Query 在事实边界内自由补充。早期曾尝试把模型总结出的开头、结尾和大量细化写法随机组合，结果文章反而更死板，还可能出现开头提出许多后文没有回应的问题，因此不再采用。该功能以后台任务运行，应以页面状态为准，不要反复点击生成。

### 数据与工程说明

分析任务只在一个客户和一个 AI 平台范围内运行，后台以任务方式执行，并通过单一分析锁避免多个引用情报任务同时争用网页抓取和模型调用。任务状态和分析结果分别保存在 `data/reference_intelligence_jobs/`、`data/reference_intelligence_tasks/`；文章正文与单篇分析缓存位于客户的知识库目录。路线按客户所属行业保存。路线合并由模型判断读者是否会觉得两种写法像同一篇文章；仅措辞不同可以合并，场景不同通常保留差异。

## 6. 客户资料解析

### 模块作用

客户资料解析把运营提供的客户资料整理为客户事实来源，并为客户资料知识库提供可编辑内容。

### 运营怎么使用

如果 AI 解析出的资料不够详细，可在同一页面使用“AI 联网扩展资料”补充；AI 解析资料和符合事实章节要求的当前联网扩展资料会一并汇总到客户资料知识库。每次重新生成联网扩展资料都会覆盖上一次联网扩展结果，而不是在旧结果后持续追加，因此应在生成后查看内容是否仍适用。知识库中的客户资料默认用于内容生产；无论介绍型还是对比型，运营都可以在内容生产页面取消本次使用。

### 数据与工程说明

上传资料、索引和缓存位于 `data/uploads/`、`data/materials_index.json`、`data/material_packages/<client_id>/` 等目录，上传源文件和最终知识库不是同一份数据。联网扩展的最新结果写入 `latest_web_supplement.md`，再次扩展时会直接替换这份文件；知识库只会吸收格式符合客户事实章节要求的联网内容。内容生成校验只接受可作为事实依据的章节；若出现 `customer_content_facts_migration_required`（HTTP 422），通常是资料含有“公开背景”等不允许直接生成的章节，应将可核验的品牌、产品、服务对象、价格或信任信息整理到允许章节后保存。

## 7. 竞品资料解析

### 模块作用

竞品资料解析整理人工收集、上传或当日高引用文章补充得到的竞品信息，并将它维护为可选择的竞品资料。

### 运营怎么使用

竞品资料解析或补充完成后，结果会默认写入竞品知识库；运营应在写入后按真实竞品名称分节审核、修订和维护，而不是把写入前的确认当作唯一审核环节。补充竞品资料时，应优先使用“当日数据整理”中的竞品资料补充：它从当前日期和平台范围内的高频引用文章提取信息，实际效果通常最好。联网搜索竞品资料可以作为线索补充，但目前结果往往较简略，不能代替前者，也应在写入知识库后人工核对。生成对比型文章时，应明确选择当次需要比较的竞品，而不是期待系统自动决定比较对象。

### 数据与工程说明

竞品资料包与客户资料包独立保存，不能因为同属一个客户而自动混用。对比型文章只读取当次明确选择的竞品，且至少需要两家；系统不会随机抽取竞品，也不会编造竞品事实。当日数据整理中的“竞品资料补充”从当前范围的高引用文章提取信息，完成后写入同一份竞品知识库。自动同步会按竞品章节和事实行合并、跳过重复内容，而不是整份覆盖已有知识库，因此人工已保存的内容通常会保留。联网竞品扩展则基于搜索结果和模型整理，当前更适合补充线索，容易出现信息简略的情况，不能把它当作竞品事实的唯一来源。

## 8. 知识库

### 模块作用

知识库保存运营确认后可长期维护的内容，不等同于原始上传资料，也不会在未选择时自动进入内容生产。除系统提示词外，编辑使用自动保存，输入停止约 800ms、失焦、增删和改名都会触发保存。

### 运营怎么使用

客户资料知识库用于维护每客户一份事实资料，可以编辑和导出 DOCX。竞品资料知识库按 `## 竞品名称` 分节维护，可以改名称、改描述、删除当前章节和导出 DOCX；竞品资料解析有时会生成事实很少的竞品，这类章节可以直接删除，也可以在生成对比型文章时不选择。删除章节后，后续同步仍可能重新补入来源内容。行业写法库按行业和文章类型维护介绍型、对比型路线，可以新建、编辑、删除和导出 DOCX。Query 场景词库按问题组与 Query 展示写作提示，可以编辑和导出 DOCX，但只能辅助理解读者表达，不能取代客户或竞品事实。质量门禁知识库维护通用与行业规则，自动保存但不支持下载。系统提示词展示经审核的提示词和变量说明，运营可以选中文本复制，但不能编辑或下载。生成文章质量不佳时，应优先检查知识库中的客户资料、竞品资料和写法路线是否足够准确、完整和贴合本次 Query，因为这些内容对最终产出的影响通常大于临时调整一句生成要求。除质量门禁和系统提示词外，提供下载功能是为了在业务需求变化时，将可用资料下载后，在授权范围内自行交给 Codex、GPT 等工具继续生文或处理。

### 数据与工程说明

客户、竞品、场景词和写法库的 DOCX 导出只包含当前可见、已保存的内容，不包含原始抓取资料、内部状态或密钥。行业写法路线按行业隔离，而不是按具体客户隔离；它用于安排文章判断主线和组织方式。自动保存由前端以约 800ms 防抖和失焦触发，服务端没有版本冲突合并机制，因此两人同时编辑同一份知识库时，后到达的保存会覆盖前一份编辑内容。质量门禁管理的是审核提醒规则，而不是某篇文章的审核结果；系统提示词也不会展示客户正文、抓取原文、密钥或最终模型请求。

## 9. 内容生产

### 模块作用

内容生产根据运营明确选择的客户资料、竞品资料、Query 和行业路线生成待人工审核的文章。

### 运营怎么使用

运营先选择客户、问题组、Query、介绍型或对比型和行业写法路线。介绍型默认使用知识库中的客户资料，运营可以在本次生成时取消；对比型同样默认使用客户资料，并需要从竞品知识库选择至少两家竞品。内容生产先围绕本次 Query 确定文章主体和读者判断主线，再从所选写法路线了解合适的组织方式，并由同一次生成完成选材、组织、解释和成文。此前曾尝试先生成简报、再由第二阶段正式生文，但文章组织过于死板，已经弃用。高频引用文章也未必被 AI 完整阅读：AI 会先生成检索提示词、召回大量文章后再选择部分精读；以豆包页面显示的二十多篇高频引用文章为例，通常只有约 6—8 篇被真正阅读，其余更多只是按关键词召回的摘要信号。因此，用高频引用文章仿写仍需结合运营经验和后续微调，不能把引用次数直接等同于文章被完整阅读或可直接套用。一次可生成 1、2 或 3 篇，结果卡片会显示使用的写法路线和 Query。生成完成后仍需人工审核，再决定是否进入发布流程。

### 数据与工程说明

请求进入后，后端会先校验客户、问题组与 Query 的对应关系，以及介绍型、对比型各自的资料边界；再按本次勾选读取客户知识库中的客户事实、内容生产页面另行上传资料的解析文本，并按运营选中的名称从竞品知识库读取竞品事实。客户知识库资料会先经过事实分节校验，不符合可用于生文的结构时会在调用模型前拦截。随后，系统按客户所属行业和文章类型抽取一条写法路线，同时读取本次 Query 的主场景词及同问题组可选场景词，组成一次性的生成包。生成包会将 Query、文章类型、标题策略、客户品牌、写法路线、客户事实、可选上传资料、已选竞品事实和场景词一并传给写作模型，由同一次模型调用直接返回文章正文。正文生成后，系统会提取标题，并将本次路线、资料范围和近期文章送入质量门禁；完成后把正文、路线上下文和门禁报告写入内容生产 SQLite。批量任务按 1、2、3 篇顺序创建，并使用发起运营账号的模型配置。遇到 `Invalid token`、401 等错误时，应优先检查发起任务的运营账号模型配置，且不得在日志、页面或聊天记录中暴露 Token。

## 10. 质量门禁

### 模块作用

质量门禁对生成文章或上传文章按通用、行业规则给出风险提醒，辅助运营完成最终人工审核。

### 运营怎么使用

运营可以查看规则提醒和对应原因，并在知识库的质量门禁模块维护通用或行业规则。面对提醒时，可修改文章、重新生成，或决定不发布；系统不会替代运营作出自动发布决定。

### 数据与工程说明

质量门禁是提醒机制，不自动替代人工审核或自动发布。检查先执行违禁词、标题品牌、元话语和近期文章相似度等代码规则，再调用模型规则；模型检查失败时只会形成警告，不会丢弃已生成文章。通用与行业规则保存在 `data/quality_gate/policy.json`，内容生产完成后会在文章记录中保存质量提醒，而门禁规则本身不提供下载。

## 11. 发布文章与分发资源管理

### 模块作用

发布文章与分发资源管理用于管理待发布文章和运营账号可用的分发资源。

### 运营怎么使用

运营可以查看、上传或处理待发布文章，也可以查看个人可用资源和资源处理状态。执行发布或下单前，必须人工确认文章内容、品牌事实、合规性和发布对象，不应把测试文章或未经审核的模型输出直接提交。

### 数据与工程说明

发布草稿、供应商订单和资源记录与内容生成共用 `data/content_generations.sqlite3`。运营个人的供应商凭据保存于 `data/user_settings/<用户名>.json`，常用资源和供应商目录也按运行数据保存；这些文件均可能含敏感信息，不能写进 Git、文档或聊天记录。下单和刷新订单状态会调用外部供应商接口，失败时不应通过直接修改本地订单状态伪造成功。

## 12. 系统设置与权限

### 模块作用

系统设置与权限管理登录、客户访问权限和模型配置。

### 运营怎么使用

运营使用个人账号登录，并在权限范围内操作所属客户。系统设置中的模型配置为全体账号共用，任一账号保存 API、Base URL、模型或联网搜索配置后都会立即应用到全系统，因此修改前应先确认配置内容。遇到无权限、模型鉴权或平台登录问题时，应记录客户、时间、平台和页面提示后反馈，而不是尝试查看其他客户或复制敏感配置。

### 数据与工程说明

客户级读取、编辑和导出必须经过访问控制，越权请求应以“未找到”处理，以避免泄露其他客户是否存在。模型设置统一位于 `data/settings.json`，调用模型时不再读取 `data/user_settings/<用户名>.json` 中遗留的模型覆盖字段；该目录仍只用于运营个人的分发平台凭据等与模型无关的配置。`.env`、上述设置文件、`*_state.json`、`*_cookies.json` 可能含密钥或登录态，禁止外传。当前云端服务仍使用部分 JSON 文件状态和 SQLite，因此 Gunicorn 保持单 worker，不能通过增加多个 worker 尝试提速。

## 当前系统边界

系统不建设平台偏好矩阵、运营经验库、自进化、自动权重优化、RAG 升级或多轮人工审核，也不恢复旧的临时选择层报告展示、全平台混合逐题矩阵、随机竞品池或两阶段内容生成。爬取、外部网页抓取和模型调用均受外部服务影响；遇到慢或失败时，应先依据页面任务状态和日志定位，而不是通过重复点击制造并发。
