# GEO Agent 使用说明 v2.4

> 多客户 GEO 情报监控 × AI 内容生产工作台
> 支持平台：豆包 / DeepSeek / 元宝 / 千问

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 或 macOS |
| Python | 3.10 及以上 |
| 网络 | 能正常访问各平台网页 |

---

## 二、首次安装

```bash
pip install -r requirements.txt
playwright install chromium
```

启动程序：
```bash
python app.py
```

浏览器打开 `http://localhost:5000`

默认会开放给同一局域网访问。启动后脚本会显示本机地址和局域网地址：

```powershell
.\启动.bat
```

同事访问脚本输出的 `http://你的电脑IPv4地址:5000`。不要把该地址暴露到公网。

---

## 三、各平台登录说明

右上角切换平台后，点击对应的登录按钮：

### 🤖 豆包
- 点击「🤖 豆包」登录按钮，在弹出浏览器中扫码/账号登录
- 程序自动检测登录成功并保存 Cookie

### 🔍 DeepSeek
- 点击「🔍 DeepSeek」登录按钮，在弹出浏览器中完成登录
- 程序会保存并复验网页登录状态
- DeepSeek 在「问题组一键爬取」中固定单窗口执行，避免多个窗口抢同一账号的生成状态

### 💎 元宝
- 点击「💎 元宝」登录按钮，在弹出浏览器中完成微信/QQ/手机号登录
- 程序检测到左侧出现「新对话」按钮即认为登录成功，自动保存状态

### 🌙 千问
- 点击「🌙 千问」登录按钮，在弹出浏览器中完成阿里云账号登录
- 程序检测到左侧出现「新建对话」按钮即认为登录成功，自动保存状态
- ⚠️ 请不要手动关闭浏览器窗口！程序会自动关闭

---

## 四、日常工作流

```
① 配置探针问题 → ② 爬取数据 → ③ 当日整理 & 深度分析 → ④ 内容生产
```

### Step 1：配置探针问题
- 进入「问题组管理」，AI 自动生成覆盖各场景的问题组
- 建议每个客户配置 2-3 个问题组，每组 5-10 题
- 生成问题前确认品牌和行业字段正确；当前 prompt 已限制问题必须围绕行业、服务场景、用户决策和 GEO 目标，避免泛生活问题

### Step 2：爬取数据
- 右上角切换目标平台，点击「问题组一键爬取」
- 「问题组一键爬取」固定单窗口执行，优先保证稳定性
- 「引用情报」自动爬取模式仍可选择并行窗口数
- 遇到人机验证：程序自动暂停等待，在浏览器完成验证后自动继续

### Step 3：当日整理
- 进入「当日数据整理」，查看今日爬取统计
- 点击「深度分析」生成平台分析报告
- 可导入内容生产模块（自动精炼为300字内容规律）

### Step 4：内容生产
- 上传客户品牌资料和发文模板（每个客户独立管理）
- 智能生成主题 → 批量生成文章（5线程并发）
- 格式优化：纯文本 / HTML富文本 / 小红书 / 知乎

---

## 五、多平台数据说明

切换右上角平台后，所有页面数据自动切换为对应平台：

| 平台 | 引用源来源 | 特点 |
|------|------|------|
| 豆包 | 回答内联引用卡片 | 数据最丰富，土巴兔/新浪家居为主 |
| DeepSeek | 网页回答与可见链接 | 引用源可能为空，回答质量较高 |
| 元宝 | 引用来源面板（点击「源」展开）| 引用结构清晰，有域名+标题 |
| 千问 | 参考来源面板（右侧） | 有序号+来源域名，结构最规整 |

---

## 六、GEO 评分说明（满分100）

| 维度 | 分值 |
|------|------|
| 品牌直接提及 | +20 |
| 引用文章含品牌 | 最高+30（每篇+10）|
| 排名位置 | 最高+25 |
| 情感倾向 | ±10~15 |
| 高权重平台 | 最高+10 |
| 提及频次 | 最高+10 |

> ⚠️ 已提及判断：只看回答**正文**中是否出现品牌名，引用标题不算

---

## 七、常见问题

**Q：Cookie 中途失效怎么办？**
A：程序会自动检测并尝试重新加载 Cookie 重试一次，无法恢复时会跳过剩余题目并提示重新登录。建议避免同一账号在多处同时使用。

**Q：爬取任务中途报错 500？**
A：已修复，TimeoutError 等异常不再导致整体崩溃，会跳过当前题目继续执行。

**Q：DeepSeek 为什么只开一个窗口？**
A：DeepSeek 对同一账号通常只允许一个对话生成。多窗口会出现一个窗口已填入问题但等待另一个窗口生成完成的现象，所以问题组爬取固定为单窗口。

**Q：为什么问题组生成的问题需要复查？**
A：问题组生成依赖品牌、行业和问题组描述。当前已加强 prompt 约束，但正式使用前仍建议运营同学先检查问题是否贴合业务场景。

**Q：元宝/千问登录后窗口马上关闭？**
A：已修复。程序现在通过检测特定页面元素（元宝：agent-dialogue__tool__login 消失；千问：新建对话按钮出现）判断登录成功，不会误判。

---

## 八、数据目录

```
data/
├── clients.json          # 客户信息
├── probe_groups.json     # 探针问题组
├── raw_records.json      # 爬取记录（含 source_platform 字段区分来源）
├── articles.json         # 生成的文章
├── settings.json         # 系统配置（含 DeepSeek API Key）
├── doubao_cookies.json   # 豆包 Cookie
├── deepseek_state.json   # DeepSeek 完整登录状态（含 localStorage）
├── yuanbao_state.json    # 元宝完整登录状态（含 localStorage）
├── qwen_state.json       # 千问完整登录状态（含 localStorage）
├── uploads/{客户id}/     # 各客户品牌资料
└── templates/{客户id}/   # 各客户发文模板
```

> ⚠️ 请定期备份 `data/` 目录！

---

## 九、版本历史

**v2.4（当前）**
- ✅ 增加工程化脚本：重启、停止、健康检查、单元测试、无人值守回归
- ✅ 增加平台登录态复验和状态区分：未登录、已过期、已验证
- ✅ DeepSeek / 豆包 / 元宝增加回答完整性保护，疑似截断不再误判成功
- ✅ 问题组爬取固定单窗口，降低多窗口抢生成状态的问题
- ✅ 问题组 AI 生成 prompt 加强行业和 GEO 目标约束
- ✅ 自动化回归报告写入 `reports/`

**v2.1**
- ✅ 新增豆包/DeepSeek/元宝/千问四平台切换
- ✅ 元宝/千问登录检测修复（使用真实 DOM class 判断）
- ✅ 所有平台使用 storage_state 保存完整登录状态（含 localStorage token）
- ✅ 数据按平台隔离，切换平台自动刷新所有页面数据
- ✅ 爬取异常不再导致整体崩溃（TimeoutError、Cookie 失效均有处理）
- ✅ 高频引用文章从 Top10 扩展至 Top20
- ✅ 跨平台 GEO 对比看板

**v2.0**
- ✅ 重复数据/空爬/人机验证等核心 Bug 修复
- ✅ 数据严格按客户隔离
- ✅ 文章生成注入真实竞品名单，抑制幻觉
- ✅ 批量生成 5 线程并发
